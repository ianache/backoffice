---
phase: "02.1-ui-system"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - portal/src/assets/main.css
  - portal/src/App.vue
  - portal/src/stores/ui.ts
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "UI System Research and Planning is documented"
    - "Theme tokens are defined in CSS variables"
  artifacts:
    - path: "portal/src/stores/ui.ts"
      provides: "Pinia store for UI state (theme)"
    - path: "portal/src/assets/theme.css"
      provides: "CSS variables for Light and Dark modes"
---

<objective>
Investigate and plan the UI alignment with Google Stitch and the implementation of the Light/Dark theme system.

Purpose: Consolidate UI todos into an actionable plan to ensure visual consistency and modern theme support before moving to User Management.

Output: Research document and initial implementation strategy for themes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create UI Store and Theme CSS tokens</name>
  <files>
    portal/src/stores/ui.ts,
    portal/src/assets/theme.css
  </files>
  <action>
**portal/src/stores/ui.ts**: Create a new Pinia store to manage the current theme state (light/dark).
**portal/src/assets/theme.css**: Define CSS variables for both modes based on the design documents in `design/`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Integrate Theme Toggle in Navbar</name>
  <files>
    portal/src/App.vue
  </files>
  <action>
Add the theme toggle component/button to the navigation bar in `App.vue`, ensuring it matches the requested positioning (left-aligned, before other icons).
  </action>
</task>

</tasks>

<success_criteria>
- Phase 2.1 is ready for execution.
- CSS variable structure is established.
- Theme switching logic is implemented in Pinia.
</success_criteria>
