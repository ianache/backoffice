---
phase: 05-rule-builder
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - portal/src/views/RuleBuilderView.vue
  - portal/src/router/index.ts
  - portal/src/components/flags/FlagDrawer.vue
  - portal/src/components/flags/FlagForm.vue
autonomous: false
requirements: [RULE-01, RULE-02, RULE-03]

must_haves:
  truths:
    - "User can navigate to /flags/:id/rules from FlagDrawer's Edit Rules button"
    - "User can see existing flag rules loaded into draggable cards on the canvas"
    - "User can add a new rule card with empty fields using the Add New Logic Block button"
    - "User can reorder rule cards by dragging the drag_indicator handle"
    - "User can see live simulator results update as they edit rules or the context JSON"
    - "User can save changes (PATCH rules + rollout) and be redirected to /flags"
    - "User can cancel and return to /flags without saving"
    - "FlagForm no longer shows JSON textarea — shows read-only label instead"
  artifacts:
    - path: "portal/src/views/RuleBuilderView.vue"
      provides: "Full-page rule editor at /flags/:id/rules"
      contains: "draggable"
    - path: "portal/src/router/index.ts"
      provides: "route rule-builder at /flags/:id/rules"
      contains: "rule-builder"
    - path: "portal/src/components/flags/FlagDrawer.vue"
      provides: "Edit Rules button navigating to rule-builder route"
      contains: "openRuleBuilder"
    - path: "portal/src/components/flags/FlagForm.vue"
      provides: "Removed JSON textarea; replaced with read-only label"
  key_links:
    - from: "portal/src/components/flags/FlagDrawer.vue"
      to: "portal/src/views/RuleBuilderView.vue"
      via: "router.push({ name: 'rule-builder', params: { id: flag.id } })"
      pattern: "rule-builder"
    - from: "portal/src/views/RuleBuilderView.vue"
      to: "portal/src/stores/flags.ts"
      via: "store.updateFlag(id, { rules, rollout, complex })"
      pattern: "updateFlag"
    - from: "portal/src/views/RuleBuilderView.vue"
      to: "portal/src/components/flags/RuleSimulator.vue"
      via: ":rules prop binding"
      pattern: "RuleSimulator"
---

<objective>
Assemble RuleBuilderView with vuedraggable canvas and simulator grid, wire the router entry, add the Edit Rules button to FlagDrawer, and remove the JSON textarea from FlagForm. Ends with an E2E human verification checkpoint.

Purpose: This is the delivery plan — all prior wave components come together into a fully operational rule builder page. RULE-01, RULE-02, RULE-03 all verified end-to-end.
Output: RuleBuilderView.vue (full page), route /flags/:id/rules, FlagDrawer navigation, FlagForm cleanup.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-rule-builder/05-CONTEXT.md
@.planning/phases/05-rule-builder/05-RESEARCH.md
@.planning/phases/05-rule-builder/05-01-SUMMARY.md
@.planning/phases/05-rule-builder/05-02-SUMMARY.md

<interfaces>
<!-- Contracts needed by this plan. Extracted from codebase. -->

From portal/src/router/index.ts (existing pattern to follow):
```typescript
{
  path: '/flags',
  name: 'flags',
  component: () => import('../views/FlagsView.vue'),
  meta: { requiresAuth: true, roles: ['PlatformAdmin', 'TenantAdmin', 'TenantOwner', 'ProductManager'], layout: 'main', title: 'Feature Flags' },
},
```

From portal/src/stores/flags.ts (existing updateFlag signature):
```typescript
async function updateFlag(id: number, payload: Partial<FlagPayload>): Promise<FeatureFlag>
// FlagPayload includes: rules?: RuleSchema[], rollout?: number, complex?: boolean
```

From portal/src/services/flags.ts:
```typescript
export interface RuleSchema {
  attribute: string
  operator: string
  value: any
  result: boolean
}
export interface FeatureFlag {
  id: number
  rules: RuleSchema[]
  rollout: number  // 0-100
  complex: boolean
  // ... other fields
}
```

From portal/src/components/flags/FlagDrawer.vue (existing emit + props):
```typescript
const props = defineProps<{ show: boolean; flag?: FeatureFlag | null }>()
const emit = defineEmits<{ close: []; saved: [flag: FeatureFlag] }>()
// Currently imports: { ref, watch } from 'vue'
// Currently imports: { useFeatureFlagsStore } from '../../stores/flags'
// FlagDrawer already uses StitchButton — use same for "Edit Rules" button
```

From portal/src/components/flags/FlagForm.vue (lines to replace):
```typescript
// Lines 25: const rulesRaw = ref('')
// Lines 74-95: JSON.parse(rulesRaw.value) validation + parsing
// Lines 92-95: rules variable set from rulesRaw
// Lines 225-232: <textarea v-model="rulesRaw" ...> + hint/error spans
// Lines 109: rules: rules, in payload
// All of the above must be removed; rules field removed from FlagPayload emitted by FlagForm
// (Rules are now managed exclusively in RuleBuilderView — FlagForm never writes rules)
```

Components created in prior waves:
- `portal/src/components/flags/RuleCard.vue` — props: { rule: RuleSchema & { _id: string }, index: number }; emits: update, delete
- `portal/src/components/flags/RuleSimulator.vue` — props: { rules: (RuleSchema & { _id: string })[] }
- vuedraggable@next — installed at `portal/node_modules/vuedraggable`

vuedraggable import pattern:
```typescript
import draggable from 'vuedraggable'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RuleBuilderView and wire router + FlagDrawer + FlagForm</name>
  <files>portal/src/views/RuleBuilderView.vue, portal/src/router/index.ts, portal/src/components/flags/FlagDrawer.vue, portal/src/components/flags/FlagForm.vue</files>
  <action>
### 1. Create portal/src/views/RuleBuilderView.vue

Full-page route component. Owns `localRules` (with `_id` extension) and `localRollout`.

Script setup:
```typescript
import { ref, computed, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import draggable from 'vuedraggable'
import { useFeatureFlagsStore } from '../stores/flags'
import type { RuleSchema } from '../services/flags'
import RuleCard from '../components/flags/RuleCard.vue'
import RuleSimulator from '../components/flags/RuleSimulator.vue'

interface RuleLocal extends RuleSchema {
  _id: string
}

const route = useRoute()
const router = useRouter()
const store = useFeatureFlagsStore()

const flagId = computed(() => Number(route.params.id))
const flag = computed(() => store.flags.find(f => f.id === flagId.value) ?? null)

const localRules = ref<RuleLocal[]>([])
const localRollout = ref(0)
const isSaving = ref(false)

onMounted(async () => {
  if (store.flags.length === 0) await store.fetchFlags()
  if (flag.value) {
    localRules.value = flag.value.rules.map(r => ({ ...r, _id: crypto.randomUUID() }))
    localRollout.value = flag.value.rollout ?? 0
  } else {
    // Flag not found — redirect back
    router.push({ name: 'flags' })
  }
})

function addRule(): void {
  localRules.value.push({
    _id: crypto.randomUUID(),
    attribute: '',
    operator: 'equals',
    value: '',
    result: true,
  })
}

function updateRule(index: number, updated: RuleLocal): void {
  // Use splice to mutate in place (not full replacement) — avoids vuedraggable reactivity pitfall
  localRules.value.splice(index, 1, updated)
}

function deleteRule(index: number): void {
  localRules.value.splice(index, 1)
}

async function saveChanges(): Promise<void> {
  isSaving.value = true
  try {
    const rules = localRules.value.map(({ _id, ...r }) => r)  // strip _id before PATCH
    await store.updateFlag(flagId.value, {
      rules,
      rollout: localRollout.value,
      complex: rules.length > 0,  // auto-set complex=true when rules exist
    })
    router.push({ name: 'flags' })
  } finally {
    isSaving.value = false
  }
}

function cancel(): void {
  router.push({ name: 'flags' })
}
```

Template layout — 12-column grid, left 8 cols canvas, right 4 cols simulator:

```html
<template>
  <div class="rule-builder-page flex flex-col h-full">
    <!-- Page header -->
    <div class="flex items-center justify-between px-6 py-4 border-b border-outline-variant bg-surface flex-shrink-0">
      <div class="flex items-center gap-3">
        <button type="button" @click="cancel" class="flex items-center gap-1 text-sm text-on-surface-variant hover:text-on-surface transition-colors">
          <span class="material-symbols-outlined text-base">arrow_back</span>
          Back
        </button>
        <span class="text-on-surface-variant">/</span>
        <h1 class="text-base font-semibold text-on-surface">Rule Builder</h1>
        <span v-if="flag" class="text-sm text-on-surface-variant">— {{ flag.name }}</span>
      </div>
      <!-- Environment tabs: decorative only -->
      <div class="flex items-center gap-1 bg-surface-container rounded-lg p-1">
        <button v-for="env in ['Production', 'Staging', 'Development']" :key="env"
          :class="['px-3 py-1 rounded text-xs font-medium transition-colors', env === 'Production' ? 'bg-surface text-on-surface shadow-sm' : 'text-on-surface-variant hover:text-on-surface']"
          type="button">
          {{ env }}
        </button>
      </div>
      <!-- Save / Cancel -->
      <div class="flex items-center gap-2">
        <button type="button" @click="cancel" class="px-4 py-2 text-sm text-on-surface-variant hover:text-on-surface transition-colors">
          Cancel
        </button>
        <button type="button" @click="saveChanges" :disabled="isSaving"
          class="px-4 py-2 bg-primary text-on-primary text-sm font-medium rounded-lg hover:opacity-90 disabled:opacity-50 transition-opacity flex items-center gap-2">
          <span v-if="isSaving" class="material-symbols-outlined text-sm animate-spin">progress_activity</span>
          Save Changes
        </button>
      </div>
    </div>

    <!-- Main content: 12-col grid -->
    <div class="flex-1 grid grid-cols-12 gap-0 overflow-hidden">
      <!-- Left: Rule canvas (8 cols) -->
      <div class="col-span-8 flex flex-col overflow-auto border-r border-outline-variant relative">
        <!-- Dotted grid background (decorative) -->
        <div class="absolute inset-0 pointer-events-none opacity-[0.03]"
          style="background-image: radial-gradient(var(--primary) 0.5px, transparent 0.5px); background-size: 20px 20px;">
        </div>
        <!-- Canvas content -->
        <div class="relative z-10 px-8 py-6 flex flex-col gap-0">
          <!-- Rollout slider -->
          <div class="mb-6 flex items-center gap-4 bg-surface-container-lowest rounded-xl border border-outline-variant px-5 py-4">
            <span class="text-sm font-medium text-on-surface whitespace-nowrap">Rollout</span>
            <input type="range" min="0" max="100" v-model.number="localRollout"
              class="flex-1 accent-primary h-2 cursor-pointer" />
            <span class="text-sm font-semibold text-primary w-10 text-right">{{ localRollout }}%</span>
          </div>

          <!-- Draggable rule cards list -->
          <draggable
            v-model="localRules"
            item-key="_id"
            handle=".drag-handle"
            :animation="200"
            ghost-class="rule-card--ghost"
          >
            <template #item="{ element, index }">
              <div>
                <!-- AND connector between cards (not before first) -->
                <div v-if="index > 0" class="flex justify-center items-center relative my-1 pointer-events-none">
                  <div class="w-px h-6 bg-outline-variant"></div>
                  <div class="absolute bg-surface-container-lowest px-3 py-0.5 rounded-full border border-outline-variant text-xs font-bold text-primary select-none">
                    AND
                  </div>
                </div>
                <RuleCard
                  :rule="element"
                  :index="index"
                  @update="updateRule(index, $event)"
                  @delete="deleteRule(index)"
                />
              </div>
            </template>
          </draggable>

          <!-- Add New Logic Block button -->
          <button type="button" @click="addRule"
            class="mt-4 w-full flex items-center justify-center gap-2 px-4 py-3 rounded-xl border-2 border-dashed border-outline-variant text-sm text-on-surface-variant hover:border-primary hover:text-primary transition-colors group">
            <span class="material-symbols-outlined text-base group-hover:text-primary">add_circle</span>
            Add New Logic Block
          </button>

          <!-- Empty state when no rules -->
          <div v-if="localRules.length === 0" class="mt-6 text-center text-sm text-on-surface-variant italic py-8 opacity-50">
            No rules yet. Add a logic block to define evaluation conditions.
          </div>
        </div>
      </div>

      <!-- Right: Simulator sidebar (4 cols) -->
      <div class="col-span-4 p-4 overflow-auto bg-surface-container-low">
        <RuleSimulator :rules="localRules" />
      </div>
    </div>
  </div>
</template>
```

Add scoped styles for ghost class:
```css
<style scoped>
.rule-card--ghost {
  opacity: 0.4;
  background-color: var(--primary-container);
}
</style>
```

### 2. Add route to portal/src/router/index.ts

Add after the `/flags` route entry (around line 51, before closing `]`):
```typescript
{
  path: '/flags/:id/rules',
  name: 'rule-builder',
  component: () => import('../views/RuleBuilderView.vue'),
  meta: {
    requiresAuth: true,
    roles: ['PlatformAdmin', 'TenantAdmin', 'TenantOwner', 'ProductManager'],
    layout: 'main',
    title: 'Rule Builder'
  },
},
```

### 3. Add "Edit Rules" button to portal/src/components/flags/FlagDrawer.vue

Add `useRouter` import:
```typescript
import { useRouter } from 'vue-router'
const router = useRouter()
```

Add openRuleBuilder function (after existing handleSave):
```typescript
function openRuleBuilder() {
  if (props.flag?.id) {
    emit('close')
    router.push({ name: 'rule-builder', params: { id: props.flag.id } })
  }
}
```

In the template, add an "Edit Rules" button in the drawer header or footer — only shown when editing an existing flag (props.flag exists and has id). Position it prominently near the save button. Use StitchButton or matching button style:
```html
<button v-if="props.flag?.id" type="button" @click="openRuleBuilder"
  class="flex items-center gap-1.5 px-4 py-2 text-sm font-medium text-primary border border-primary rounded-lg hover:bg-primary hover:text-on-primary transition-colors">
  <span class="material-symbols-outlined text-base">rule</span>
  Edit Rules
</button>
```

### 4. Clean up portal/src/components/flags/FlagForm.vue

Remove the JSON textarea rules section:
- Remove `const rulesRaw = ref('')` (line ~25)
- Remove `rulesRaw.value = flag.rules.length ? JSON.stringify(flag.rules, null, 2) : ''` from the watch (line ~42)
- Remove `rulesRaw.value = ''` from the reset block (line ~52)
- Remove the rules JSON validation block in validate() (lines ~74-80: the try/catch JSON.parse)
- Remove `errors.value.rules` references from errors ref type and usage
- Remove `let rules = undefined` and the JSON.parse block (lines ~92-95)
- Remove `rules: rules,` from the emit payload (line ~109)
- Replace the textarea section (lines ~222-232) with a read-only informational block:
  ```html
  <div class="form-group">
    <label class="form-label">Rules</label>
    <p class="text-sm text-on-surface-variant bg-surface-container rounded-lg px-3 py-2 border border-outline-variant">
      Rules are managed in the
      <span class="text-primary font-medium">Rule Builder</span>
      — open it from the drawer header when editing a flag.
    </p>
  </div>
  ```

Note: Keep `rulesRaw` removed entirely. FlagForm should no longer include `rules` in the FlagPayload it emits — rules are managed only via RuleBuilderView's PATCH call.
  </action>
  <verify>
    <automated>cd portal && pnpm build 2>&1 | grep -E "error|ERROR" | grep -v "node_modules" | head -20 || echo "BUILD_OK"</automated>
  </verify>
  <done>RuleBuilderView.vue exists and imports draggable, RuleCard, RuleSimulator. router/index.ts contains rule-builder route. FlagDrawer.vue contains openRuleBuilder function. FlagForm.vue no longer contains rulesRaw or the rules JSON textarea. TypeScript build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Rule Builder verification</name>
  <action>Human verifies the complete Rule Builder feature works end-to-end: navigation from FlagDrawer, draggable cards, chip input, live simulator, save/cancel, and FlagForm cleanup.</action>
  <what-built>
    Complete Rule Builder page at /flags/:id/rules with:
    - Draggable logic block cards (vuedraggable, drag_indicator handle)
    - 4-field card layout: attribute (text), operator (select), value (chip input or text), result (toggle)
    - AND connector between cards (decorative)
    - Live Simulator sidebar updating in real-time as rules and context change
    - Rollout percentage slider (0-100%)
    - Save Changes (PATCH to backend) and Cancel (back to /flags) buttons
    - "Edit Rules" button in FlagDrawer for existing flags
    - FlagForm rules JSON textarea replaced with read-only label
  </what-built>
  <how-to-verify>
    1. Start the portal dev server: `cd portal && pnpm dev`
    2. Login as bo.admin / Backoffice1! and navigate to /flags
    3. Open an existing flag in the drawer — confirm "Edit Rules" button is visible
    4. Click "Edit Rules" — confirm navigation to /flags/{id}/rules
    5. Confirm the Rule Builder page loads with the flag's name in the header
    6. Confirm existing rules (if any) are loaded as cards; if none, canvas shows empty state
    7. Click "Add New Logic Block" — confirm a new card appears
    8. Edit the card: type "country" in Attribute, select "in" from Operator — confirm ChipTagInput appears for Value; type "PE" + Enter — confirm chip appears
    9. Change Operator to "equals" — confirm value field switches to plain text input with "PE" preserved
    10. Drag a card using the left drag_indicator handle — confirm cards reorder
    11. Check Live Simulator: edit test context JSON to `{"country": "PE"}` — confirm matched rule highlights
    12. Test invalid JSON in simulator (type `{bad`) — confirm error message appears, no crash
    13. Move the rollout slider — confirm percentage updates
    14. Click "Save Changes" — confirm redirect to /flags and toast shows saved
    15. Reopen the flag in FlagDrawer — confirm rules section shows "Rules are managed in the Rule Builder" label (not JSON textarea)
    16. Click Cancel on Rule Builder — confirm redirect back to /flags without saving
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd portal && pnpm build` — no new TypeScript errors
2. `grep "rule-builder" portal/src/router/index.ts` — route exists
3. `grep "openRuleBuilder" portal/src/components/flags/FlagDrawer.vue` — button wired
4. `grep "rulesRaw" portal/src/components/flags/FlagForm.vue` — should return nothing (removed)
5. `grep "draggable" portal/src/views/RuleBuilderView.vue` — vuedraggable imported
6. Human E2E verification approved
</verification>

<success_criteria>
- User can navigate to /flags/:id/rules via FlagDrawer's "Edit Rules" button
- User can create, edit, reorder (drag & drop), and delete rule cards
- Live Simulator evaluates rules in real-time and shows matched rule + result badge
- Save Changes PATCHes rules + rollout to backend; Cancel returns without saving
- FlagForm no longer exposes JSON textarea for rules
- TypeScript build clean throughout
- Human E2E checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-rule-builder/05-03-SUMMARY.md`
</output>
