---
phase: "06-stitch-ui-implementation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - portal/package.json
  - portal/vite.config.ts
  - portal/src/assets/tailwind.css
  - portal/tailwind.config.js
  - portal/src/components/ui/StitchButton.vue
  - portal/src/components/ui/StitchTextField.vue
  - portal/src/plugins/material.ts
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Tailwind CSS is configured and usable in the portal"
    - "@material/web components can be used in Vue templates without warnings"
    - "Design tokens from theme.css are mapped to Tailwind utility classes"
  artifacts:
    - path: "portal/tailwind.config.js"
      provides: "Tailwind configuration mapping theme tokens"
    - path: "portal/src/components/ui/StitchButton.vue"
      provides: "Atomic wrapper for md-filled-button"
    - path: "portal/src/components/ui/StitchTextField.vue"
      provides: "Atomic wrapper for md-outlined-text-field"
---

<objective>
Establish the technical foundation for the Stitch UI Implementation by installing required libraries, configuring Tailwind CSS to match our design tokens, and creating the first set of atomic component wrappers.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stitch-ui-implementation/06-RESEARCH.md
@portal/src/assets/theme.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Tailwind CSS</name>
  <files>portal/package.json, portal/tailwind.config.js, portal/src/assets/tailwind.css, portal/src/main.ts</files>
  <action>
    1. Install Tailwind CSS and its dependencies in the portal package: `npm install -D tailwindcss autoprefixer postcss`.
    2. Initialize Tailwind: `npx tailwindcss init -p` (ensure it's in the portal directory).
    3. Configure `tailwind.config.js` to map colors from `portal/src/assets/theme.css` variables (e.g., `primary: 'var(--primary)'`, `surface: 'var(--surface)'`).
    4. Create `portal/src/assets/tailwind.css` with `@tailwind base; @tailwind components; @tailwind utilities;`.
    5. Import `tailwind.css` in `portal/src/main.ts`.
  </action>
  <verify>
    <automated>pnpm --filter portal exec tailwindcss -i portal/src/assets/tailwind.css -o portal/dist/output.css</automated>
  </verify>
  <done>Tailwind CSS is installed and configured to use theme.css tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Configure @material/web and create atomic wrappers</name>
  <files>portal/package.json, portal/vite.config.ts, portal/src/plugins/material.ts, portal/src/components/ui/StitchButton.vue, portal/src/components/ui/StitchTextField.vue</files>
  <action>
    1. Install `@material/web`: `npm install @material/web`.
    2. Update `portal/vite.config.ts` to include `isCustomElement: (tag) => tag.startsWith('md-')` in the Vue plugin template options.
    3. Create `portal/src/plugins/material.ts` to import the necessary web components (e.g., `@material/web/button/filled-button`, `@material/web/textfield/outlined-text-field`).
    4. Create `portal/src/components/ui/StitchButton.vue`:
       - Wrap `<md-filled-button>`.
       - Map Tailwind classes for spacing and custom Stitch styling (e.g., `rounded-lg`).
       - Use CSS variables for colors.
    5. Create `portal/src/components/ui/StitchTextField.vue`:
       - Wrap `<md-outlined-text-field>`.
       - Ensure it supports v-model and standard attributes.
  </action>
  <verify>
    <automated>pnpm --filter portal run build</automated>
  </verify>
  <done>@material/web is integrated and base atomic components are created.</done>
</task>

<task type="auto">
  <name>Task 3: Setup Playwright for visual regression</name>
  <files>portal/package.json, portal/playwright.config.ts, portal/tests/visual/smoke.spec.ts</files>
  <action>
    1. Install Playwright: `npm install -D @playwright/test`.
    2. Create `portal/playwright.config.ts` configured for the portal (port 5173).
    3. Create a smoke test `portal/tests/visual/smoke.spec.ts` that navigates to the login page and takes a screenshot.
  </action>
  <verify>
    <automated>pnpm --filter portal exec playwright test portal/tests/visual/smoke.spec.ts</automated>
  </verify>
  <done>Playwright is configured and can run visual tests.</done>
</task>

</tasks>

<verification>
Check if `portal/dist` contains the compiled CSS with Tailwind utilities and if the build succeeds with the new Material web components.
</verification>

<success_criteria>
- Build passes without Vue compiler warnings about custom elements.
- Tailwind utility classes successfully reference CSS variables from theme.css.
- Playwright can execute a basic screenshot test.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stitch-ui-implementation/06-01-SUMMARY.md`
</output>
