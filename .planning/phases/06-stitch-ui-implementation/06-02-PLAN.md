---
phase: "06-stitch-ui-implementation"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - portal/src/components/layout/MainLayout.vue
  - portal/src/components/layout/AuthLayout.vue
  - portal/src/router/index.ts
  - portal/src/App.vue
autonomous: false
requirements: [UI-04]

must_haves:
  truths:
    - "Portal uses a Navigation Rail and App Bar following Stitch design"
    - "Internal pages are wrapped in MainLayout"
    - "Auth pages are wrapped in AuthLayout"
  artifacts:
    - path: "portal/src/components/layout/MainLayout.vue"
      provides: "Layout with Nav Rail and App Bar"
    - path: "portal/src/components/layout/AuthLayout.vue"
      provides: "Simplified layout for login/auth pages"
---

<objective>
Implement the layout shell and navigation components according to the Stitch design specifications, establishing the structural framework for all internal and authentication pages.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stitch-ui-implementation/06-RESEARCH.md
@portal/src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Layout components</name>
  <files>portal/src/components/layout/MainLayout.vue, portal/src/components/layout/AuthLayout.vue</files>
  <action>
    1. Create `portal/src/components/layout/MainLayout.vue`:
       - Implement a 72px Navigation Rail (GCP-style) on the left.
       - Implement an App Bar (top) with the theme toggle and user profile.
       - Use `@material/web` components for the rail items (e.g., `<md-navigation-tab>`).
       - Ensure high-density spacing (Stitch standards).
    2. Create `portal/src/components/layout/AuthLayout.vue`:
       - Simple centered container for authentication forms.
       - Implement the background and branding as per Stitch login design.
  </action>
  <verify>
    <automated>pnpm --filter portal run build</automated>
  </verify>
  <done>Layout components are created with Stitch-compliant navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Update Router and App.vue to use Layouts</name>
  <files>portal/src/router/index.ts, portal/src/App.vue</files>
  <action>
    1. Refactor `portal/src/router/index.ts` to use nested routes or meta fields to assign layouts to views.
    2. Update `portal/src/App.vue` to use a dynamic layout wrapper (e.g., `<component :is="layout">`) or multiple `<router-view>` setups.
    3. Ensure the theme toggle state from Pinia is correctly propagated to the new layouts.
  </action>
  <verify>
    <automated>pnpm --filter portal run build</automated>
  </verify>
  <done>The application uses the new layout system for all routes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>New Stitch Layout Shell (Nav Rail + App Bar)</what-built>
  <how-to-verify>
    1. Run the portal: `pnpm --filter portal dev`.
    2. Log in and verify the navigation rail is visible and functional.
    3. Verify the theme toggle still works and updates the new layout components.
    4. Check responsiveness and high-density spacing.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual verification of the layout shell in the browser, plus ensuring no build errors.
</verification>

<success_criteria>
- Navigation Rail follows Stitch 72px rail pattern.
- Layout switching between Auth and Main works correctly based on route.
- High-density spacing is visible in the navigation and header.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stitch-ui-implementation/06-02-SUMMARY.md`
</output>
