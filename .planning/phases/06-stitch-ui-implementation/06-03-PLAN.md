---
phase: "06-stitch-ui-implementation"
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - portal/src/views/LoginView.vue
  - portal/tests/visual/login.spec.ts
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "Login page matches Stitch design specifications"
    - "Login functionality (email/password) remains intact"
  artifacts:
    - path: "portal/src/views/LoginView.vue"
      provides: "Stitch-designed login page"
---

<objective>
Implement the custom login page following the Google Stitch design specifications, ensuring a professional and coherent first-impression for the platform.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stitch-ui-implementation/06-RESEARCH.md
@portal/src/views/LoginView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign LoginView.vue</name>
  <files>portal/src/views/LoginView.vue</files>
  <action>
    1. Refactor `portal/src/views/LoginView.vue` to use Stitch design patterns:
       - Use `StitchTextField.vue` for email and password inputs.
       - Use `StitchButton.vue` for the submit button.
       - Implement the card-based layout with Stitch elevation tokens.
       - Add the product logo and branding as specified in the Stitch design.
    2. Maintain connection to the existing `auth` store for login logic.
    3. Ensure error messages follow Stitch notification/inline error patterns.
  </action>
  <verify>
    <automated>pnpm --filter portal run build</automated>
  </verify>
  <done>Login page is implemented with Stitch design components and layout.</done>
</task>

<task type="auto">
  <name>Task 2: Add visual regression test for Login</name>
  <files>portal/tests/visual/login.spec.ts</files>
  <action>
    1. Create a Playwright test `portal/tests/visual/login.spec.ts`.
    2. Capture screenshots of the login page in both Light and Dark modes.
    3. Assert that key elements (inputs, buttons) are visible and properly styled.
  </action>
  <verify>
    <automated>pnpm --filter portal exec playwright test portal/tests/visual/login.spec.ts</automated>
  </verify>
  <done>Visual regression tests for the login page are in place.</done>
</task>

</tasks>

<verification>
Run the visual regression tests and check the generated screenshots to ensure alignment with the Stitch design.
</verification>

<success_criteria>
- Login page accurately reflects the design from the research (UI-03).
- Login functionality works as before (verified by manual or existing auth tests).
- Visual tests pass for both themes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stitch-ui-implementation/06-03-SUMMARY.md`
</output>
