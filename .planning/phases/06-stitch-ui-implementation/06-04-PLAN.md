---
phase: "06-stitch-ui-implementation"
plan: "04"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - portal/src/views/TenantsView.vue
  - portal/src/components/tenants/TenantTable.vue
  - portal/src/components/tenants/TenantDrawer.vue
  - portal/tests/visual/internal.spec.ts
autonomous: true
requirements: [UI-04]

must_haves:
  truths:
    - "Internal pages follow the high-density Stitch layout"
    - "Tenant management components use Stitch atomic components"
  artifacts:
    - path: "portal/src/views/TenantsView.vue"
      provides: "Refactored Tenants view with Stitch design"
---

<objective>
Refactor existing internal pages, specifically the Tenant Management views, to align with the high-density Stitch design system, ensuring consistency across all management features.
</objective>

<execution_context>
@C:/Users/ianache/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stitch-ui-implementation/06-RESEARCH.md
@portal/src/views/TenantsView.vue
@portal/src/components/tenants/TenantTable.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor TenantsView and Table</name>
  <files>portal/src/views/TenantsView.vue, portal/src/components/tenants/TenantTable.vue</files>
  <action>
    1. Update `portal/src/views/TenantsView.vue` to use Stitch spacing and typography.
    2. Refactor `portal/src/components/tenants/TenantTable.vue`:
       - Use `@material/web` checkboxes for selection.
       - Implement high-density row styling (compact mode).
       - Use Stitch action menu patterns for row actions.
       - Ensure the table layout matches the 'Tenant Management' design (UI-04).
  </action>
  <verify>
    <automated>pnpm --filter portal run build</automated>
  </verify>
  <done>Tenants view and table are refactored to Stitch standards.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TenantDrawer and Forms</name>
  <files>portal/src/components/tenants/TenantDrawer.vue, portal/src/components/tenants/TenantForm.vue</files>
  <action>
    1. Update `portal/src/components/tenants/TenantDrawer.vue` to use Stitch elevation tokens and drawer patterns.
    2. Replace standard HTML inputs with `StitchTextField.vue` in all tenant forms.
    3. Use `StitchButton.vue` for all form actions.
  </action>
  <verify>
    <automated>pnpm --filter portal run build</automated>
  </verify>
  <done>Tenant management forms and drawer are updated with Stitch components.</done>
</task>

<task type="auto">
  <name>Task 3: Add visual regression test for Internal Pages</name>
  <files>portal/tests/visual/internal.spec.ts</files>
  <action>
    1. Create a Playwright test `portal/tests/visual/internal.spec.ts`.
    2. Capture screenshots of the Tenants view and the Tenant drawer.
    3. Assert correct layout and component styling in both themes.
  </action>
  <verify>
    <automated>pnpm --filter portal exec playwright test portal/tests/visual/internal.spec.ts</automated>
  </verify>
  <done>Visual regression tests for internal pages are in place.</done>
</task>

</tasks>

<verification>
Run visual tests and manually inspect the Tenants management UI to ensure it feels like a native Google enterprise console.
</verification>

<success_criteria>
- Internal pages follow the 'Tenant Management' design from research (UI-04).
- High-density spacing is consistently applied.
- All interactive elements use @material/web or Stitch atomic components.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stitch-ui-implementation/06-04-SUMMARY.md`
</output>
