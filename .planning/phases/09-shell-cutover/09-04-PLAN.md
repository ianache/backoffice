---
phase: 09-shell-cutover
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified: []
autonomous: false
requirements:
  - MUI-01
  - MUI-02
  - MUI-03

must_haves:
  truths:
    - "Hard refresh on /stub resolves to StubView.vue (not /unauthorized)"
    - "Vue, Pinia, Vue Router, and Axios each appear exactly once in federation shared scope"
    - "If mui-stub is stopped and Shell is restarted, Shell still mounts and shows RemoteErrorBoundary at /stub"
  artifacts:
    - path: "microuis/mui-stub/dist/assets/remoteEntry.js"
      provides: "Served federation entry at http://localhost:5175/assets/remoteEntry.js"
  key_links:
    - from: "portal (Shell)"
      to: "http://localhost:5175/assets/remoteEntry.js"
      via: "loadMicroUIRoutes() dynamic import at boot"
      pattern: "mui-stub/routes"
---

<objective>
Verify the full Phase 9 federation loop end-to-end: Shell boots with stub remote connected, singleton deduplication confirmed, error boundary works when stub is unreachable.

Purpose: Success criteria for Phase 9 are visual/functional — requires human confirmation that hard refresh, singleton deduplication, and error boundary all work correctly in the browser.

Output: Human approval that Phase 9 success criteria 1, 2, and 3 are all satisfied.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-shell-cutover/09-02-SUMMARY.md
@.planning/phases/09-shell-cutover/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Shell and start servers for verification</name>
  <files></files>
  <action>
Prepare both the Shell and stub for browser verification. Two terminals are needed:

**Terminal 1 — Stub remote (must be pre-built before Shell loads it):**
```bash
cd microuis/mui-stub
pnpm build           # Produces dist/assets/remoteEntry.js
pnpm preview         # Serves dist/ on http://localhost:5175
```

**Terminal 2 — Shell (federation host, can use vite dev):**
```bash
cd portal
pnpm dev             # Starts vite dev server on http://localhost:5173
```

Verify the stub is reachable:
```bash
curl http://localhost:5175/assets/remoteEntry.js | head -5
# Should return JavaScript content (not 404)
```

Confirm in the Shell dev server console output: no `Failed to fetch` errors for remoteEntry.js.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:5175/assets/remoteEntry.js</automated>
  </verify>
  <done>Both servers running; remoteEntry.js returns 200; Shell dev server shows no federation fetch errors in console</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify federation loop — singleton deduplication, hard refresh, error boundary</name>
  <action>Human verification of the three Phase 9 success criteria in the browser. No automated action — this checkpoint confirms the visual/functional behavior Claude cannot observe.</action>
  <what-built>
Phase 9 Shell Cutover: portal refactored as Module Federation host; mui-stub remote serves a /stub route; RemoteErrorBoundary shown when remote unreachable.
  </what-built>
  <how-to-verify>
**Step 1 — Verify success criterion 1: Hard refresh on deep route**
1. Open http://localhost:5173 in browser; log in with Keycloak
2. Navigate to /stub (should load StubView content: "Stub Domain" heading)
3. While on /stub, press Ctrl+Shift+R (hard refresh)
4. Confirm: page stays on /stub and shows StubView — does NOT redirect to /unauthorized

**Step 2 — Verify success criterion 2: Singleton deduplication**
1. Open browser DevTools → Console
2. Run: `Object.keys(__federation__.initShareScope).filter(k => ['vue','pinia','vue-router','axios'].includes(k))`
3. Confirm: each of the 4 keys appears exactly once
4. Alternative check: `Object.keys(__federation__.initShareScope)` — should not show 'vue' twice

**Step 3 — Verify success criterion 3: Error boundary when remote unreachable**
1. Stop the mui-stub preview server (Ctrl+C on Terminal 1)
2. Hard refresh the Shell at http://localhost:5173
3. Navigate to /stub (or hard refresh on /stub)
4. Confirm: Shell mounts successfully (login + sidebar visible)
5. Confirm: /stub shows the RemoteErrorBoundary card with "Stub Domain is unavailable"
6. Confirm: the Stub Domain nav item is visually dimmed/disabled in the sidebar
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 criteria pass, or describe any issue found</resume-signal>
</task>

</tasks>

<verification>
All three Phase 9 success criteria verified by human:
1. Hard refresh on /stub resolves to StubView (no race condition to /unauthorized)
2. vue/pinia/vue-router/axios each appear once in federation shared scope
3. Shell mounts when stub is down; RemoteErrorBoundary shown at /stub; nav item dimmed
</verification>

<success_criteria>
- Hard refresh on /stub → StubView renders (success criterion 1)
- federation shared scope shows exactly one instance of vue, pinia, vue-router, axios (success criterion 2)
- With stub server stopped: Shell mounts, /stub shows RemoteErrorBoundary, nav dimmed (success criterion 3)
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/09-shell-cutover/09-04-SUMMARY.md`
</output>
