---
phase: 10-mui-tenants-security
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - portal/src/router/index.ts
  - microuis/mui-security/package.json
  - microuis/mui-tenants/package.json
autonomous: true
gap_closure: true
requirements:
  - MUI-04
  - MUI-05

must_haves:
  truths:
    - "Shell loads mui-tenants and mui-security routes at runtime via loadMicroUIRoutes()"
    - "Running pnpm preview in mui-security serves on port 5174 (not 5175)"
    - "Running pnpm preview in mui-tenants serves on port 5176 (not 5175)"
    - "Default redirect goes to /tenants (not /stub) when authenticated"
  artifacts:
    - path: "portal/src/router/index.ts"
      provides: "Active REMOTE_MANIFEST entries for mui-security and mui-tenants"
      contains: "mui-security.*mui-tenants"
    - path: "microuis/mui-security/package.json"
      provides: "Correct preview port 5174"
      contains: "--port 5174"
    - path: "microuis/mui-tenants/package.json"
      provides: "Correct preview port 5176"
      contains: "--port 5176"
  key_links:
    - from: "portal/src/router/index.ts REMOTE_MANIFEST"
      to: "loadMicroUIRoutes()"
      via: "activeRemotes filter on envVar"
      pattern: "VITE_REMOTE_SECURITY.*VITE_REMOTE_TENANTS"
---

<objective>
Activate the Shell's awareness of mui-tenants and mui-security by uncommenting their REMOTE_MANIFEST entries, and fix the preview port mismatch in both MUI package.json files so developers can run all remotes simultaneously without conflicts.

Purpose: The REMOTE_MANIFEST entries were left commented with "// Phase 10:" prefix during Phase 9 scaffolding. Without this fix, loadMicroUIRoutes() never registers the two new remotes regardless of what is built. The port mismatch means both MUIs would conflict at 5175 when previewed, mismatching Shell .env expectations of 5174 and 5176.
Output: Shell router activates mui-security and mui-tenants; preview ports align with vite.config.ts and Shell .env.example.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mui-tenants-security/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate REMOTE_MANIFEST entries and update default redirect</name>
  <files>portal/src/router/index.ts</files>
  <action>
Edit portal/src/router/index.ts lines 26-27. The current content is:

```
  // Phase 10: { name: 'mui-security', envVar: 'VITE_REMOTE_SECURITY', displayName: 'Access Management', pathPrefix: 'users' },
  // Phase 10: { name: 'mui-tenants', envVar: 'VITE_REMOTE_TENANTS', displayName: 'Tenant Management', pathPrefix: 'tenants' },
```

Change to (remove the `// Phase 10: ` prefix from both lines):

```
  { name: 'mui-security', envVar: 'VITE_REMOTE_SECURITY', displayName: 'Access Management', pathPrefix: 'users' },
  { name: 'mui-tenants', envVar: 'VITE_REMOTE_TENANTS', displayName: 'Tenant Management', pathPrefix: 'tenants' },
```

Also update the default redirect in the routes array (currently `redirect: '/stub'`) to `redirect: '/tenants'` so authenticated users land on the primary domain.

Do NOT change the importRemote() switch — it already handles all three cases (mui-stub, mui-security, mui-tenants) and is correct.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('portal/src/router/index.ts','utf8');const hasSecurityEntry=c.includes(\"{ name: 'mui-security'\");const hasTenantsEntry=c.includes(\"{ name: 'mui-tenants'\");const noComment=!c.includes('// Phase 10:');console.log('security entry active:',hasSecurityEntry,'tenants entry active:',hasTenantsEntry,'no comment prefix:',noComment);if(!hasSecurityEntry||!hasTenantsEntry||!noComment)process.exit(1);"</automated>
  </verify>
  <done>Both REMOTE_MANIFEST entries are active (no comment prefix), and the default redirect points to /tenants.</done>
</task>

<task type="auto">
  <name>Task 2: Fix preview port in mui-security and mui-tenants package.json</name>
  <files>microuis/mui-security/package.json, microuis/mui-tenants/package.json</files>
  <action>
In microuis/mui-security/package.json, change the preview script from:
  `"preview": "vite preview --port 5175"`
to:
  `"preview": "vite preview --port 5174"`

In microuis/mui-tenants/package.json, change the preview script from:
  `"preview": "vite preview --port 5175"`
to:
  `"preview": "vite preview --port 5176"`

These ports match vite.config.ts preview.port values (5174 for mui-security, 5176 for mui-tenants) and the Shell .env.example entries (VITE_REMOTE_SECURITY=http://localhost:5174, VITE_REMOTE_TENANTS=http://localhost:5176).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const sec=JSON.parse(fs.readFileSync('microuis/mui-security/package.json','utf8'));const ten=JSON.parse(fs.readFileSync('microuis/mui-tenants/package.json','utf8'));console.log('mui-security preview:',sec.scripts.preview,'mui-tenants preview:',ten.scripts.preview);if(!sec.scripts.preview.includes('5174')||!ten.scripts.preview.includes('5176'))process.exit(1);"</automated>
  </verify>
  <done>mui-security preview runs on 5174, mui-tenants preview runs on 5176 — no port conflict when both run simultaneously, matching Shell .env.example.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full activation chain:
- portal/src/router/index.ts REMOTE_MANIFEST has 3 entries (mui-stub, mui-security, mui-tenants), none commented
- Default route redirects to /tenants
- mui-security/package.json preview uses --port 5174
- mui-tenants/package.json preview uses --port 5176
</verification>

<success_criteria>
- Shell REMOTE_MANIFEST activates mui-security (port 5174) and mui-tenants (port 5176)
- No "// Phase 10:" comment prefix remains in router/index.ts
- Port values in package.json scripts match vite.config.ts preview.port and Shell .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/10-mui-tenants-security/10-04-SUMMARY.md`
</output>
