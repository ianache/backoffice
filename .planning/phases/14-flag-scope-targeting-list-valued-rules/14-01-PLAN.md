---
phase: 14-flag-scope-targeting-list-valued-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/domains/companies/__init__.py
  - backend/app/domains/companies/models.py
  - backend/app/domains/companies/schemas.py
  - backend/app/domains/companies/service.py
  - backend/app/domains/companies/router.py
  - backend/app/main.py
  - backend/alembic/env.py
  - backend/alembic/versions/d003_create_companies_table.py
  - backend/tests/test_companies_router.py
  - bff/src/routes/companies.ts
  - bff/src/index.ts
autonomous: true
requirements: [CMP-01]

must_haves:
  truths:
    - "POST /companies/ creates a company with slug id, name, status, tenant_id and returns 201"
    - "GET /companies/ returns only the caller's tenant companies for TenantAdmin/TenantOwner, all companies for PlatformAdmin"
    - "Duplicate company id returns 409, non-privileged roles get 403"
    - "BFF proxies /companies to backend with X-User-Roles and X-User-Tenant-Id headers"
  artifacts:
    - path: "backend/app/domains/companies/router.py"
      provides: "POST/GET/PATCH /companies endpoints with role + tenant isolation"
    - path: "backend/alembic/versions/d003_create_companies_table.py"
      provides: "companies table migration (revision d003, revises d002)"
    - path: "bff/src/routes/companies.ts"
      provides: "BFF proxy route requireAuth-only forwarding identity headers"
    - path: "backend/tests/test_companies_router.py"
      provides: "DB-free tests for schema validation, role checks, tenant filter"
  key_links:
    - from: "backend/app/main.py"
      to: "app.domains.companies.router"
      via: "app.include_router(companies_router)"
      pattern: "companies_router"
    - from: "bff/src/index.ts"
      to: "bff/src/routes/companies.ts"
      via: "app.use('/companies', companiesRouter)"
      pattern: "companiesRouter"
    - from: "backend/alembic/env.py"
      to: "app.domains.companies.models"
      via: "model import for metadata registration"
      pattern: "from app.domains.companies.models import Company"
---

<objective>
Create the Companies catalog backend: new `companies` table (Alembic d003), a `companies` domain mirroring the Products domain (models/schemas/service/router), router registration in main.py, and a BFF proxy route. PlatformAdmin manages all companies; TenantAdmin/TenantOwner manage only their own tenant's companies.

Purpose: `flags.company_id` needs a real catalog to target. This is the foundation for the company-scope combobox in FlagForm (Plan 14-05) and the Companies UI (Plan 14-06).
Output: working `/companies` CRUD API reachable through the BFF, with tests.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-flag-scope-targeting-list-valued-rules/14-CONTEXT.md

Templates (read before implementing — copy their structure):
@backend/app/domains/products/models.py
@backend/app/domains/products/schemas.py
@backend/app/domains/products/service.py
@backend/app/domains/products/router.py
@backend/alembic/versions/d002_add_test_context.py
@bff/src/routes/flags.ts
</context>

<interfaces>
Established patterns this plan must follow:

From `backend/app/domains/products/router.py` (template):
- `dependencies=[Depends(verify_internal_secret)]` on the APIRouter
- IntegrityError caught in router → `HTTPException(409)` after `await db.rollback()`
- Roles parsed from `x_user_roles: str = Header(...)` as comma-separated list

From `backend/app/domains/feature_flags/router.py`:
- Tenant identity from `x_user_tenant_id: str = Header(default='')`

From `bff/src/routes/flags.ts` (BFF header forwarding — products.ts does NOT forward tenant id, flags.ts does):
```typescript
proxyReq.setHeader('X-User-Tenant-Id', (req as any).user?.tenantId ?? '')
```

From `backend/alembic/env.py` (model registration — append after products import):
```python
from app.domains.products.models import Product, TenantSubscription, FlagProduct  # noqa: F401
```

Alembic head is `d002` (verified). New revision: `d003`, `down_revision = 'd002'`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Companies domain (models, schemas, service, router) + main.py registration + tests</name>
  <files>backend/app/domains/companies/__init__.py, backend/app/domains/companies/models.py, backend/app/domains/companies/schemas.py, backend/app/domains/companies/service.py, backend/app/domains/companies/router.py, backend/app/main.py, backend/tests/test_companies_router.py</files>
  <behavior>
    Tests in backend/tests/test_companies_router.py (DB-free, same style as test_feature_flags_router.py — test pure helpers and Pydantic validators directly, no TestClient):
    - CompanyCreate accepts id 'acme_01', rejects 'Bad Slug!' and '' (regex ^[a-z0-9_]{1,50}$ raises ValueError)
    - CompanyCreate requires tenant_id (pydantic ValidationError when missing)
    - CompanyUpdate has no 'id' and no 'tenant_id' fields (assert 'id' not in CompanyUpdate.model_fields and 'tenant_id' not in CompanyUpdate.model_fields)
    - _require_companies_role(['PlatformAdmin']) passes; (['TenantAdmin']) passes; (['TenantOwner']) passes; (['TenantViewer']) raises HTTPException 403; ([]) raises 403
    - _tenant_filter_for(['PlatformAdmin'], 't1') returns None (sees all); _tenant_filter_for(['TenantAdmin'], 't1') returns 't1'
    - _check_create_tenant(['TenantAdmin'], payload_tenant_id='t2', own_tenant='t1') raises 403; same args with ['PlatformAdmin'] passes; ['TenantAdmin'] with payload 't1' / own 't1' passes
  </behavior>
  <action>
    Create `backend/app/domains/companies/` mirroring the products domain:

    1. `models.py` — `Company(Base)`, `__tablename__ = "companies"`:
       - `id: Mapped[str] = mapped_column(String(50), primary_key=True)` (user-defined slug, immutable)
       - `name: Mapped[str] = mapped_column(String(255), nullable=False)`
       - `status: Mapped[str] = mapped_column(String(20), server_default='active', nullable=False)`
       - `tenant_id: Mapped[str] = mapped_column(String(100), nullable=False, index=True)` — NO FK (Keycloak-managed pattern, per [07-01] decision)
       - `created_at`/`updated_at` with `server_default=func.now()` (+ `onupdate=func.now()` for updated_at), exactly like Product.
       Import `Base` from the same module products/models.py imports it from (check that import line and copy it).

    2. `schemas.py` — `CompanyCreate` (id, name, status='active', tenant_id; `field_validator('id')` with regex `^[a-z0-9_]{1,50}$` copied from ProductCreate), `CompanyUpdate` (name/status only, both Optional — id and tenant_id immutable), `CompanyResponse` (all columns, `model_config = ConfigDict(from_attributes=True)`). No JSON/TEXT fields → no model_validator needed.

    3. `service.py` — pure async CRUD, no HTTP semantics (07-02 pattern):
       - `create_company(db, payload)` — construct Company, add, commit, refresh, return
       - `get_company(db, company_id)` — scalar_one_or_none
       - `list_companies(db, tenant_id=None, status=None)` — `select(Company)`, `.where(Company.tenant_id == tenant_id)` if tenant_id, `.where(Company.status == status)` if status
       - `update_company(db, company_id, payload)` — `model_dump(exclude_unset=True)`, setattr loop, commit, refresh; return None if not found

    4. `router.py` — `APIRouter(prefix="/companies", tags=["companies"], dependencies=[Depends(verify_internal_secret)])`. Extract THREE module-level pure helpers (so tests need no TestClient):
       - `_require_companies_role(roles: list[str]) -> None` — raise 403 unless `{'PlatformAdmin','TenantAdmin','TenantOwner'} & set(roles)`
       - `_tenant_filter_for(roles: list[str], own_tenant: str) -> Optional[str]` — None for PlatformAdmin, else own_tenant
       - `_check_create_tenant(roles, payload_tenant_id, own_tenant) -> None` — non-PlatformAdmin creating/updating a company whose tenant_id != own_tenant → 403 "Cannot manage companies for another tenant"
       Endpoints:
       - `POST /` (201): parse roles from `x_user_roles: str = Header(...)`, `x_user_tenant_id: str = Header(default='')`; `_require_companies_role`; `_check_create_tenant`; call service; catch IntegrityError → rollback → 409 `f"Company with id '{payload.id}' already exists"` (products pattern)
       - `GET /` (`status: Optional[str] = Query(None)`): `_require_companies_role`; `list_companies(db, tenant_id=_tenant_filter_for(...), status=status)`
       - `PATCH /{company_id}`: `_require_companies_role`; fetch company, 404 if missing; if non-PlatformAdmin and `company.tenant_id != x_user_tenant_id` → 403; then `update_company`

    5. `__init__.py` — empty file.

    6. `backend/app/main.py` — add `from app.domains.companies.router import router as companies_router` and `app.include_router(companies_router)` next to the products router lines.

    Write the failing tests FIRST, then implement until green.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_companies_router.py -x -q && python -c "from app.main import app"</automated>
  </verify>
  <done>All test_companies_router.py tests pass; app imports cleanly with companies router registered.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration d003 (companies table) + env.py model registration</name>
  <files>backend/alembic/versions/d003_create_companies_table.py, backend/alembic/env.py</files>
  <action>
    1. Create `backend/alembic/versions/d003_create_companies_table.py` with `revision = 'd003'`, `down_revision = 'd002'` (mirror d002_add_test_context.py header structure):
       - upgrade(): `op.create_table('companies', sa.Column('id', sa.String(50), primary_key=True), sa.Column('name', sa.String(255), nullable=False), sa.Column('status', sa.String(20), server_default='active', nullable=False), sa.Column('tenant_id', sa.String(100), nullable=False), sa.Column('created_at', sa.DateTime(), server_default=sa.func.now(), nullable=False), sa.Column('updated_at', sa.DateTime(), server_default=sa.func.now(), nullable=False))` then `op.create_index('ix_companies_tenant_id', 'companies', ['tenant_id'])`
       - downgrade(): drop index, drop table.
       Single additive revision — brand-new table, no MySQL 5.6 expand/backfill/cleanup needed (13-01 precedent).
    2. In `backend/alembic/env.py` add `from app.domains.companies.models import Company  # noqa: F401` after the products models import so the table registers with Base.metadata.
    3. Apply: `cd backend && alembic upgrade head`. If the dev DB is unreachable, do NOT fail the task on that step — verify with `alembic heads` showing d003 and note in the SUMMARY that the migration must be applied when the DB is up.
  </action>
  <verify>
    <automated>cd backend && alembic heads | grep d003 && alembic upgrade head</automated>
  </verify>
  <done>`alembic heads` reports d003 as the single head; migration applies cleanly (or is documented as pending DB availability).</done>
</task>

<task type="auto">
  <name>Task 3: BFF /companies proxy route</name>
  <files>bff/src/routes/companies.ts, bff/src/index.ts</files>
  <action>
    1. Create `bff/src/routes/companies.ts` mirroring `bff/src/routes/products.ts` (requireAuth ONLY — no requireRole; backend enforces roles via X-User-Roles, per resolved open question #3), with two differences:
       - `pathRewrite: (path) => `/companies${path}``
       - forward the tenant header (flags.ts pattern, needed for tenant isolation): `proxyReq.setHeader('X-User-Tenant-Id', (req as any).user?.tenantId ?? '')` in addition to X-Internal-Secret, X-User-Sub, X-User-Roles.
    2. Register in `bff/src/index.ts`: import `companiesRouter` and add `app.use('/companies', companiesRouter)` after the products line, with the same "express.json() intentionally NOT applied" comment convention.
  </action>
  <verify>
    <automated>cd bff && pnpm build</automated>
  </verify>
  <done>bff compiles; /companies route registered with requireAuth and forwards X-User-Roles + X-User-Tenant-Id + X-Internal-Secret.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -x -q` — full backend suite green (no regressions).
- `cd bff && pnpm build` — BFF compiles.
- `cd backend && alembic heads` → d003.
</verification>

<success_criteria>
- Companies domain exists with the same file structure as products domain.
- Role matrix enforced: PlatformAdmin (all tenants), TenantAdmin/TenantOwner (own tenant only), others 403.
- Duplicate slug → 409; companies list filterable by status.
- BFF /companies reachable with requireAuth and full identity-header forwarding.
</success_criteria>

<output>
After completion, create `.planning/phases/14-flag-scope-targeting-list-valued-rules/14-01-SUMMARY.md`
</output>
