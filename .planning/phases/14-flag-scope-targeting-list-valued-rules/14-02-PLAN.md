---
phase: 14-flag-scope-targeting-list-valued-rules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/domains/feature_flags/schemas.py
  - backend/app/domains/feature_flags/router.py
  - backend/app/domains/sdk/service.py
  - backend/app/domains/sdk/router.py
  - backend/tests/test_feature_flags_router.py
  - backend/tests/test_sdk_bootstrap.py
autonomous: true
requirements: [TGT-02, TGT-03]

must_haves:
  truths:
    - "Creating a flag with scope=product and no product_id returns 422 (same for tenant/company)"
    - "PATCH /flags/{id} accepts scope, tenant_id, product_id, company_id so legacy flags can be retrofitted with a target"
    - "When scope changes via PATCH, the non-matching target columns are cleared (mutual exclusivity)"
    - "GET /sdk/bootstrap includes company-scoped flags (with target fields in each entry) and excludes tenant/product flags whose target does not match the requesting client"
    - "POST /sdk/evaluate can resolve product- and company-scoped flags (no longer starved by the tenant-only list_flags pre-filter)"
    - "Flags edited without touching scope/target (e.g. toggling enabled) are NOT subject to the new validation — legacy behavior unchanged"
  artifacts:
    - path: "backend/app/domains/feature_flags/schemas.py"
      provides: "FlagCreate scope-target model_validator; FlagUpdate gains scope/tenant_id/product_id/company_id"
      contains: "validate_scope_target"
    - path: "backend/app/domains/sdk/service.py"
      provides: "_flag_matches_target() per-scope dispatch + target fields in bootstrap entries"
      contains: "_flag_matches_target"
    - path: "backend/app/domains/sdk/router.py"
      provides: "/sdk/evaluate fetches flags unfiltered then filters by flag_key"
  key_links:
    - from: "backend/app/domains/feature_flags/router.py"
      to: "merged flag+payload state"
      via: "update_flag 422 validation when scope/target present in payload"
      pattern: "_validate_update_target"
    - from: "backend/app/domains/sdk/service.py"
      to: "bootstrap response entries"
      via: "tenant_id/product_id/company_id keys per flag entry"
      pattern: "company_id"
---

<objective>
Enforce scope targeting in the backend: server-side validation that non-global flags carry their target (create + update), FlagUpdate support for retrofitting targets onto legacy flags, per-scope dispatch in `bootstrap_flags()` (fixing the company-scope exclusion gap), target fields exposed in bootstrap entries for SDK-side checks, and the `/sdk/evaluate` unfiltered-fetch fix.

Purpose: TGT-02 (target persisted and validated) and TGT-03 (SDK bootstrap/evaluate enforce scope+target). A flag scoped to product=X must never activate for product=Y.
Output: backend enforcement layer complete; SDK clients get target metadata in bootstrap (consumed by Plan 14-03).
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-flag-scope-targeting-list-valued-rules/14-CONTEXT.md
@backend/app/domains/feature_flags/schemas.py
@backend/app/domains/feature_flags/router.py
@backend/app/domains/sdk/service.py
@backend/app/domains/sdk/router.py
@backend/tests/test_sdk_bootstrap.py
</context>

<interfaces>
Current state (verified by direct read):

`FlagUpdate` (schemas.py:31-42) has NO scope/tenant_id/product_id/company_id fields — `update_flag()`'s `model_dump(exclude_unset=True)` silently drops them today.

`bootstrap_flags()` current filter (sdk/service.py:21-30) — scope-blind OR clauses; `scope='company', tenant_id=None` matches NO clause → company flags are excluded from bootstrap today:
```python
flags = [
    f for f in flags
    if f.environment == environment
    and (
        f.scope == 'global'
        or (f.tenant_id is not None and str(f.tenant_id) == str(tenant_id))
        or (f.scope == 'product' and f.product_id == product_id)
    )
    and (f.product_id is None or f.product_id == product_id)
]
```

`/sdk/evaluate` (sdk/router.py:33): `flags = await list_flags(db, tenant_id=payload.user.get('tenant_id'))` — list_flags' tenant filter is `(tenant_id == X) | (scope == 'global')`, which starves product/company-scoped flags (tenant_id NULL) out of remote evaluation.

`evaluate_flag()` (feature_flags/service.py:54-98) — DO NOT TOUCH. It already does per-scope candidate matching including `flag.company_id == context.get('company_id')`, and its `None == None` legacy behavior is locked ("no change to legacy evaluation for flags without target").

Test helper in test_sdk_bootstrap.py (`make_flag`) currently lacks `tenant_id`/`company_id` kwargs — extend it with `tenant_id=None, company_id=None` defaults.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FlagCreate/FlagUpdate target validation + update merged-state enforcement</name>
  <files>backend/app/domains/feature_flags/schemas.py, backend/app/domains/feature_flags/router.py, backend/tests/test_feature_flags_router.py</files>
  <behavior>
    Tests appended to backend/tests/test_feature_flags_router.py (pure schema/helper tests, existing file style):
    - FlagCreate(scope='product', product_id=None, ...) raises ValidationError "product_id is required"
    - FlagCreate(scope='tenant', tenant_id=None) raises; FlagCreate(scope='company', company_id=None) raises
    - FlagCreate(scope='global') with no targets is valid; FlagCreate(scope='product', product_id='backoffice') is valid
    - FlagUpdate has fields scope, tenant_id, product_id, company_id (assert in model_fields)
    - _validate_update_target(current, update_data) helper: 
      - update_data={'enabled': True} on a legacy flag (scope='company', company_id=None) → no exception (legacy untouched)
      - update_data={'scope': 'product'} with merged product_id=None → HTTPException 422
      - update_data={'product_id': 'backoffice'} on flag with scope='product' → passes
      - update_data={'scope': 'tenant', 'tenant_id': 't1'} → passes AND returns cleaned dict with product_id=None, company_id=None (mutual exclusivity)
  </behavior>
  <action>
    1. `schemas.py`:
       - Add to `FlagCreate` a `@model_validator(mode='after') def validate_scope_target(self)`: scope=='tenant' requires tenant_id, 'product' requires product_id, 'company' requires company_id (raise ValueError with the field name). Global requires nothing.
       - Add to `FlagUpdate`: `scope: Optional[str] = None`, `tenant_id: Optional[str] = None`, `product_id: Optional[str] = None`, `company_id: Optional[str] = None`. NO validator on FlagUpdate itself (partial updates must not trip it — validation happens on merged state in the router).
    2. `router.py` — add module-level helper `_validate_update_target(flag, update_data: dict) -> dict`:
       - If none of {'scope','tenant_id','product_id','company_id'} are in update_data → return update_data unchanged (legacy edits untouched — locked decision).
       - Compute effective scope = update_data.get('scope', flag.scope) and effective targets merged from flag + update_data.
       - If effective scope is non-global and its required target is empty → raise HTTPException 422 ("{field} is required when scope is '{scope}'").
       - If 'scope' in update_data: force the two non-matching target keys to None in the returned dict (e.g. scope='tenant' → product_id=None, company_id=None; scope='global' → all three None). Mutual exclusivity per CONTEXT.md.
       - Return the (possibly augmented) update_data dict.
       In the `update_flag` endpoint: after the existing `_check_scope_permission(flag.scope, ...)`, additionally call `_check_scope_permission(payload.scope, roles, "update")` when `payload.scope` is set and differs from flag.scope (changing INTO a scope also needs permission). Then call `_validate_update_target(flag, payload.model_dump(exclude_unset=True))` and pass the validated dict onward. Simplest wiring: keep calling `service.update_flag(db, flag_id, payload)` but rebuild payload via `FlagUpdate(**validated_dict)` — note `FlagUpdate(**d)` marks all keys in d as set, which is exactly what we need for the forced-None clearing to persist.
    3. Update the docstring comment in `RuleSchema.operator` if touched — otherwise leave the rest of schemas.py alone.
    Write failing tests first, then implement.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feature_flags_router.py -x -q</automated>
  </verify>
  <done>Create rejects non-global flags without target (422 via Pydantic); update validates merged state only when scope/target touched; scope change clears the other two target columns; legacy no-target edits still work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bootstrap_flags() per-scope dispatch + target fields in entries</name>
  <files>backend/app/domains/sdk/service.py, backend/tests/test_sdk_bootstrap.py</files>
  <behavior>
    Tests appended to backend/tests/test_sdk_bootstrap.py (extend `make_flag` with tenant_id=None, company_id=None kwargs first; new class TestBootstrapTargetFiltering using the existing monkeypatch pattern):
    - global flag → included
    - scope='tenant', tenant_id='t1', request tenant_id='t1' → included; request tenant_id='t2' → excluded
    - scope='product', product_id='p1', request product_id='p1' → included; request product_id='p2' → excluded
    - scope='product', product_id=None (legacy, no target) → excluded (same as current filter behavior — clause 3 requires equality today)
    - scope='company', company_id='acme', tenant_id=None → INCLUDED (the company-scope gap fix)
    - scope='company', tenant_id='t1', request tenant_id='t2' → excluded
    - every returned entry contains keys 'tenant_id', 'product_id', 'company_id' mirroring the flag columns (e.g. result['my_flag']['company_id'] == 'acme')
    - environment mismatch still excluded
  </behavior>
  <action>
    In `backend/app/domains/sdk/service.py`:
    1. Add module-level `def _flag_matches_target(f, tenant_id, product_id) -> bool` implementing per-scope dispatch (research §3):
       - 'global' → True
       - 'tenant' → `f.tenant_id is not None and str(f.tenant_id) == str(tenant_id)`
       - 'product' → `f.product_id is not None and f.product_id == product_id`
       - 'company' → `f.tenant_id is None or str(f.tenant_id) == str(tenant_id)` (company target is per-user context, NOT per-SDK-client — include in bootstrap, the evaluator checks company_id; only filter by tenant when the flag also has one)
       - unknown scope → False
    2. Replace the existing OR-clause filter in `bootstrap_flags()` with:
       `flags = [f for f in flags if f.environment == environment and _flag_matches_target(f, tenant_id, product_id)]`
       (Drop the trailing `and (f.product_id is None or f.product_id == product_id)` clause — dispatch covers it.)
    3. Extend each `result[flag.name]` entry with the target fields so SDK local evaluators can enforce company scope (consumed by Plan 14-03):
       `"tenant_id": flag.tenant_id, "product_id": flag.product_id, "company_id": getattr(flag, 'company_id', None)` (getattr keeps the SimpleNamespace test helpers working).
    Use `getattr(f, ...)` defensively in `_flag_matches_target` too (test fixtures are SimpleNamespace).
    Write failing tests first.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_sdk_bootstrap.py -x -q</automated>
  </verify>
  <done>Company-scoped flags appear in bootstrap; tenant/product flags filtered strictly by target; entries carry tenant_id/product_id/company_id; all pre-existing bootstrap tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /sdk/evaluate fetch-unfiltered fix</name>
  <files>backend/app/domains/sdk/router.py, backend/tests/test_sdk_bootstrap.py</files>
  <behavior>
    New class TestSdkEvaluateScoping in backend/tests/test_sdk_bootstrap.py — call the route coroutine `evaluate(payload, db=None)` directly (import from app.domains.sdk.router), monkeypatching `app.domains.feature_flags.service.list_flags` and `app.domains.sdk.service.resolve_segment_members`:
    - product-scoped flag (scope='product', product_id='p1', tenant_id=None, enabled=1, default_val=1, rules='[]') + payload.user={'product_id': 'p1'} → result True (previously unreachable: list_flags tenant filter starved it)
    - company-scoped flag (company_id='acme') + payload.user={'company_id': 'acme'} → True; payload.user={'company_id': 'other'} → False
    - flags helper objects need: id, name, scope, tenant_id, product_id, company_id, enabled, default_val, rules (reuse/extend make_flag)
  </behavior>
  <action>
    In `backend/app/domains/sdk/router.py` `evaluate()`: change
    `flags = await list_flags(db, tenant_id=payload.user.get('tenant_id'))`
    to
    `flags = await list_flags(db)`
    and keep the `f.name == payload.flag_key` filter — per-name candidate sets are tiny, and `evaluate_flag()` already does scope+target candidate matching (company/product/tenant) against `context`. Add a comment explaining WHY unfiltered: the tenant-only pre-filter excludes product/company-scoped flags whose tenant_id is NULL (research Pitfall 5).
    For the EvaluateRequest payload in tests, build it with `EvaluateRequest(flag_key=..., user={...})` imported from app.domains.sdk.schemas.
    Note: `evaluate()` route has a `verify_sdk_secret` router dependency — calling the coroutine directly bypasses it; that is fine (auth is tested separately in this file already).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_sdk_bootstrap.py tests/test_feature_flags_eval.py -x -q</automated>
  </verify>
  <done>POST /sdk/evaluate resolves product- and company-scoped flags; mismatched company context evaluates False; full eval suite green.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -x -q` — full backend suite green.
- Manual sanity (optional): create flag scope=product without product_id via curl → 422.
</verification>

<success_criteria>
- Non-global flag creation without target is impossible server-side; updates can retrofit targets onto legacy flags.
- bootstrap excludes wrong-target tenant/product flags, includes company-scoped flags with target metadata.
- /sdk/evaluate enforces scope+target for all four scopes via evaluate_flag's existing candidate matching.
- Zero behavior change for legacy flags when scope/target fields are not touched.
</success_criteria>

<output>
After completion, create `.planning/phases/14-flag-scope-targeting-list-valued-rules/14-02-SUMMARY.md`
</output>
