---
phase: 14-flag-scope-targeting-list-valued-rules
plan: 05
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - microuis/mui-feature-flags/src/services/lookups.ts
  - microuis/mui-feature-flags/src/services/flags.ts
  - microuis/mui-feature-flags/src/components/flags/flagFormModel.ts
  - microuis/mui-feature-flags/src/components/flags/flagFormModel.test.ts
  - microuis/mui-feature-flags/src/components/flags/FlagForm.vue
autonomous: true
requirements: [TGT-01, TGT-02]

must_haves:
  truths:
    - "Selecting scope=product in FlagForm shows a Product combobox (active products, label 'Name (id)'); tenant and company scopes show their own comboboxes"
    - "Saving a non-global flag without selecting a target is blocked with an inline validation error"
    - "Switching scope clears the previously selected target; the payload carries exactly one target id and explicit null for the other two"
    - "Editing an existing flag pre-selects its stored target; editing a legacy non-global flag without target forces selection before save"
    - "Empty catalog shows a helpful message (e.g. 'No active companies — create one in /companies') instead of an empty select"
  artifacts:
    - path: "microuis/mui-feature-flags/src/services/lookups.ts"
      provides: "listTenantsLookup/listProductsLookup/listCompaniesLookup via shell/api BFF calls"
      exports: ["listTenantsLookup", "listProductsLookup", "listCompaniesLookup"]
    - path: "microuis/mui-feature-flags/src/components/flags/flagFormModel.ts"
      provides: "pure validateFlagTarget + buildTargetFields helpers (unit-testable without mounting)"
      exports: ["validateFlagTarget", "buildTargetFields"]
    - path: "microuis/mui-feature-flags/src/components/flags/FlagForm.vue"
      provides: "conditional target comboboxes wired to lookups + validation + scope-switch clearing"
  key_links:
    - from: "FlagForm.vue"
      to: "services/lookups.ts"
      via: "fetch on scope selection"
      pattern: "listCompaniesLookup|listProductsLookup|listTenantsLookup"
    - from: "FlagForm.vue handleSave payload"
      to: "backend FlagCreate/FlagUpdate target columns"
      via: "tenant_id/product_id/company_id in FlagPayload (others null)"
      pattern: "buildTargetFields"
---

<objective>
Add per-scope target comboboxes to FlagForm: product/tenant/company selects fed by new local lookup services (BFF `/products/`, `/tenants/`, `/companies/`), with required-target validation, scope-switch clearing, mutual-exclusivity payload (explicit nulls), and "Name (id)" option labels.

Purpose: TGT-01 — the UI finally sets the target columns that `evaluate_flag()` has matched on since Phase 4; combined with Plan 14-02's backend validation this completes TGT-02 end-to-end.
Output: FlagForm creates/edits flags with persisted scope targets.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-flag-scope-targeting-list-valued-rules/14-CONTEXT.md
@microuis/mui-feature-flags/src/components/flags/FlagForm.vue
@microuis/mui-feature-flags/src/services/flags.ts
@microuis/mui-tenants/src/services/products.ts
</context>

<interfaces>
FlagForm.vue today: form state refs (name/scope/description/...), `watch(() => props.flag)` reset block, `validate()` with `errors` ref, `handleSave()` building FlagPayload — the payload currently OMITS tenant_id/product_id/company_id entirely.

FlagDrawer.vue passes the payload straight to `flagsStore.createFlag/updateFlag` → `POST/PATCH /flags` — no drawer changes needed.

Lookup endpoints (via `shell/api` axios singleton, same import as services/flags.ts):
- `GET /tenants/` → `Tenant[]` where `id: number, name: string` (BFF route is PlatformAdmin-only — see 403 fallback below)
- `GET /products/?status=active` → `Product[]` where `id: string (slug), name: string`
- `GET /companies/?status=active` → `Company[]` where `id: string (slug), name: string, tenant_id: string` (from Plan 14-01; backend already tenant-isolates results per caller role)

Option labels (locked): products/companies → `${name} (${id})`; tenants → `${name} (#${id})`. Persisted value: the id (tenants: `String(t.id)` — flags.tenant_id is a string column).

403 fallback for tenants (resolved open question): `_check_scope_permission` allows TenantAdmin/TenantOwner to create tenant-scope flags, but BFF `/tenants` requires PlatformAdmin. On a 403/error from `GET /tenants/`, fall back to a single option built from the logged-in user's own tenant via `useUserContext()` (`shell/useUserContext`, already declared in microuis/mui-feature-flags/src/env.d.ts and used by RuleSimulator.vue): `{ id: ctx.tenant_id, name: 'My tenant' }` (skip if ctx.tenant_id is empty → show empty-state message).

FlagPayload (services/flags.ts) — widen target fields to allow explicit null (mutual exclusivity: "los otros dos viajan null"):
```typescript
tenant_id?: string | null
product_id?: string | null
company_id?: string | null
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lookups service + flagFormModel pure helpers + tests</name>
  <files>microuis/mui-feature-flags/src/services/lookups.ts, microuis/mui-feature-flags/src/services/flags.ts, microuis/mui-feature-flags/src/components/flags/flagFormModel.ts, microuis/mui-feature-flags/src/components/flags/flagFormModel.test.ts</files>
  <behavior>
    flagFormModel.test.ts (pure functions, no Vue mounting, vitest):
    - validateFlagTarget('global', {tenantId:'', productId:'', companyId:''}) → null (no error)
    - validateFlagTarget('product', {productId:''...}) → error string mentioning product
    - validateFlagTarget('tenant', {tenantId:''...}) → error; validateFlagTarget('company', {companyId:''...}) → error
    - validateFlagTarget('product', {productId:'backoffice'}) → null
    - buildTargetFields('product', {tenantId:'t1', productId:'p1', companyId:'c1'}) → {tenant_id: null, product_id: 'p1', company_id: null} (only the active scope's target survives; stale values from previous scope selections are nulled)
    - buildTargetFields('global', anything) → all three null
    - buildTargetFields('tenant', {tenantId:'12'}) → {tenant_id: '12', product_id: null, company_id: null}
  </behavior>
  <action>
    1. Create `src/services/lookups.ts` using `import api from 'shell/api'`:
       ```typescript
       export interface LookupOption { id: string; name: string }
       export async function listProductsLookup(): Promise<LookupOption[]>   // GET /products/, params {status:'active'}, map to {id, name}
       export async function listCompaniesLookup(): Promise<LookupOption[]>  // GET /companies/, params {status:'active'}, map to {id, name}
       export async function listTenantsLookup(): Promise<LookupOption[]>    // GET /tenants/, map to {id: String(t.id), name: t.name}
       ```
       Each tolerates `{items}` wrappers like mui-tenants listProducts does (`Array.isArray(data) ? data : data.items ?? []`). Do NOT catch errors here — callers handle fallback.
    2. Create `src/components/flags/flagFormModel.ts` with the two pure exports:
       - `validateFlagTarget(scope: string, sel: {tenantId: string; productId: string; companyId: string}): string | null` — returns an error message ('Select a product for product-scoped flags', etc.) when the active scope's selection is empty; null otherwise.
       - `buildTargetFields(scope, sel): {tenant_id: string | null; product_id: string | null; company_id: string | null}` — only the active scope's id is non-null.
    3. `src/services/flags.ts`: widen FlagPayload's tenant_id/product_id/company_id to `string | null` (interfaces block).
    Tests first.
  </action>
  <verify>
    <automated>cd microuis/mui-feature-flags && pnpm test -- flagFormModel</automated>
  </verify>
  <done>Pure helpers green; lookups compile; FlagPayload accepts explicit nulls.</done>
</task>

<task type="auto">
  <name>Task 2: FlagForm.vue comboboxes + validation + scope-switch clearing</name>
  <files>microuis/mui-feature-flags/src/components/flags/FlagForm.vue</files>
  <action>
    Script setup additions:
    - Refs: `tenantId = ref('')`, `productId = ref('')`, `companyId = ref('')`; option lists `tenantOptions/productOptions/companyOptions = ref<LookupOption[]>([])`; loading + loaded flags per catalog; extend `errors` type with `target?: string`.
    - In the `watch(() => props.flag)` reset block: when editing, seed `tenantId.value = flag.tenant_id ?? ''` etc.; when creating, reset all three to ''.
    - `watch(scope, (newScope, oldScope))`: if oldScope && newScope !== oldScope → clear ALL three selection refs (locked: switching scope clears the previous target). Then lazily fetch the catalog for the new scope if not already loaded:
      - product → `listProductsLookup()`
      - company → `listCompaniesLookup()`
      - tenant → `listTenantsLookup()` wrapped in try/catch; on error use the `useUserContext()` fallback (interfaces block) — import `{ useUserContext } from 'shell/useUserContext'`.
      On edit-mode initial load (immediate), also trigger the fetch for the flag's current scope so the combobox is populated with the pre-selected value visible.
    - `validate()`: after the existing name/scope checks, `errors.value.target = validateFlagTarget(scope.value, {tenantId: tenantId.value, productId: productId.value, companyId: companyId.value}) ?? undefined` and fail validation when set (import from './flagFormModel').
    - `handleSave()`: spread `...buildTargetFields(scope.value, {...})` into the payload (after `scope:`). FlagPayload now carries explicit nulls for the two inactive targets — required so PATCH clears stale columns server-side (Plan 14-02 reads explicitly-set nulls).
    Template — insert directly below the Scope form-field, one conditional block per scope (all share the same structure):
    ```html
    <div v-if="scope === 'product'" class="form-field">
      <label class="form-label">Product <span class="text-error">*</span></label>
      <select v-model="productId" class="form-input" :class="{ 'form-input--error': errors.target }">
        <option value="" disabled>Select product</option>
        <option v-for="o in productOptions" :key="o.id" :value="o.id">{{ o.name }} ({{ o.id }})</option>
      </select>
      <span v-if="!productsLoading && productOptions.length === 0" class="form-hint">No active products — create one in the Products catalog.</span>
      <span v-if="errors.target" class="form-error">{{ errors.target }}</span>
    </div>
    ```
    Mirror for tenant (label `{{ o.name }} (#{{ o.id }})`, empty-state "No tenants available") and company (empty-state "No active companies — create one in /companies"). Show the error span only inside the active scope's block.
  </action>
  <verify>
    <automated>cd microuis/mui-feature-flags && pnpm test && pnpm build</automated>
  </verify>
  <done>Combobox appears per non-global scope with Name (id) labels and active-only options; save blocked without target; scope switch clears selection; payload has exactly one non-null target.</done>
</task>

<task type="auto">
  <name>Task 3: end-to-end wiring check against live backend</name>
  <files>microuis/mui-feature-flags/src/components/flags/FlagForm.vue</files>
  <action>
    Integration sanity pass (automated where possible):
    1. `cd microuis/mui-feature-flags && pnpm build` — federation bundle builds.
    2. With backend running (if available in session): `curl -s -X POST http://localhost:8000/flags/ -H "Content-Type: application/json" -H "X-Internal-Secret: <from backend/.env>" -H "X-User-Roles: PlatformAdmin" -H "X-User-Sub: test" -d '{"name":"tgt_smoke","scope":"product"}'` → expect 422 (validator from 14-02), then with `"product_id":"backoffice"` → 201; delete the smoke flag afterwards via DELETE /flags/{id}. If the backend is not running, skip and note in SUMMARY (the 14-02 pytest suite already covers this contract).
    3. Fix any payload/contract mismatches surfaced (e.g. null handling) in FlagForm.vue only — backend files are owned by Plan 14-02 and must not be modified here.
  </action>
  <verify>
    <automated>cd microuis/mui-feature-flags && pnpm build</automated>
  </verify>
  <done>Bundle builds; create-flag contract verified (422 without target / 201 with) or explicitly deferred to phase UAT with reason in SUMMARY.</done>
</task>

</tasks>

<verification>
- `cd microuis/mui-feature-flags && pnpm test && pnpm build` green.
- Pure-helper tests prove: target required per scope, mutual exclusivity nulls.
</verification>

<success_criteria>
- TGT-01: combobox per scope with locked label format, active-only catalogs, required validation, scope-switch clearing.
- TGT-02 (UI half): created/edited flags persist exactly one target id; legacy flags get a target on next edit.
</success_criteria>

<output>
After completion, create `.planning/phases/14-flag-scope-targeting-list-valued-rules/14-05-SUMMARY.md`
</output>
