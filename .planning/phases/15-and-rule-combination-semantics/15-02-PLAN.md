---
phase: 15-and-rule-combination-semantics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/sdk-js/src/evaluator.ts
  - sdk/sdk-js/src/types.ts
  - sdk/sdk-js/tests/evaluator.test.ts
  - sdk/sdk-python/src/backoffice_sdk/evaluator.py
  - sdk/sdk-python/tests/test_evaluator.py
autonomous: true
requirements: [AND-01]

must_haves:
  truths:
    - "sdk-js evaluateFlag with entry.rule_combination_mode='and' and 2+ rules returns true only when ALL rules match, false otherwise"
    - "sdk-python evaluate_flag implements identical 'and' semantics"
    - "Per-rule result field is ignored in 'and' mode in both SDKs"
    - "Legacy entries (rule_combination_mode missing/null/'first_match') evaluate byte-identically to before — full existing suites green unmodified"
    - "In 'and' mode with non-empty rules, segments and default_val are NOT consulted when any rule fails; empty rules fall through exactly like legacy"
  artifacts:
    - path: "sdk/sdk-js/src/types.ts"
      provides: "FlagEntry.rule_combination_mode optional field"
      contains: "rule_combination_mode"
    - path: "sdk/sdk-js/src/evaluator.ts"
      provides: "AND branch in evaluateFlag before the first-match loop"
      contains: "rule_combination_mode"
    - path: "sdk/sdk-python/src/backoffice_sdk/evaluator.py"
      provides: "AND branch in evaluate_flag before the first-match loop"
      contains: "rule_combination_mode"
  key_links:
    - from: "sdk/sdk-js/src/evaluator.ts evaluateFlag"
      to: "FlagEntry.rule_combination_mode (bootstrap field from Plan 15-01)"
      via: "mode resolution with ?? 'first_match' fallback"
      pattern: "rule_combination_mode"
    - from: "sdk/sdk-python/src/backoffice_sdk/evaluator.py evaluate_flag"
      to: "entry dict 'rule_combination_mode' key"
      via: "entry.get('rule_combination_mode') or 'first_match'"
      pattern: "rule_combination_mode"
---

<objective>
Port the AND combination mode to both SDK local evaluators (sdk-js evaluateFlag, sdk-python evaluate_flag) with semantics identical to the backend, consuming the `rule_combination_mode` bootstrap entry field.

Purpose: AND-01 evaluator parity (evaluators 2 and 3 of 4; backend is Plan 15-01, useRuleSimulator is Plan 15-03).
Output: both SDKs branch on the mode; legacy cached payloads (no mode key) unchanged; suites + typecheck green.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-and-rule-combination-semantics/15-CONTEXT.md
@sdk/sdk-js/src/evaluator.ts
@sdk/sdk-js/src/types.ts
@sdk/sdk-python/src/backoffice_sdk/evaluator.py
</context>

<interfaces>
LOCKED AND SEMANTICS (identical to Plan 15-01 backend spec):
- mode = entry.rule_combination_mode ?? 'first_match' (missing key, null, undefined, '' => 'first_match'; legacy cached bootstrap payloads have no key and must behave exactly as before).
- mode == 'and' AND rules non-empty: return whether EVERY rule matches (evaluateRule / evaluate_rule per rule, existing fail-closed semantics). Per-rule `result` ignored. On any failure return false WITHOUT consulting segments or default_val.
- mode == 'and' AND rules empty: fall through to the existing segment checks / default_val exactly like today (vacuous AND must not change no-rules entries).
- mode 'first_match'/absent: existing first-match loop byte-identical.
- Company-scope guard (TGT-03, Phase 14) stays where it is — BEFORE the mode branch, order unchanged.

sdk-js (evaluator.ts — insert between the company guard and the existing `for (const rule of entry.rules)` loop):
```typescript
const mode = entry.rule_combination_mode ?? 'first_match'
if (mode === 'and' && entry.rules.length > 0) {
  // AND mode: true only if ALL rules match; per-rule `result` is ignored (AND-01).
  return entry.rules.every((rule) => evaluateRule(rule, user))
}
```
Also update the evaluateFlag doc comment to document both modes.

sdk-js types.ts — FlagEntry gains (optional so legacy cached payloads still type-check, mirroring the 14-03 company_id precedent):
```typescript
rule_combination_mode?: string | null
```

sdk-python (evaluator.py — insert between the company guard and the existing `for rule in entry.get('rules', [])` loop):
```python
mode = entry.get('rule_combination_mode') or 'first_match'
rules = entry.get('rules', [])
if mode == 'and' and rules:
    # AND mode: True only if ALL rules match; per-rule `result` is ignored (AND-01).
    return all(evaluate_rule(rule, user) for rule in rules)
```
(The legacy loop below can keep iterating `entry.get('rules', [])` or reuse `rules` — keep the diff minimal.) Update the docstring.
</interfaces>

<git_hygiene>
The working tree contains UNRELATED uncommitted user changes — in this plan's subtree: sdk/sdk-js/src/client.ts, sdk/sdk-js/tests/cache.test.ts, sdk/sdk-js/tests/client.test.ts (plus others outside: backend/app/domains/feature_flags/router.py, backend/app/domains/sdk/ws_router.py, bff/src/routes/sdk.ts, portal/*, documentations/). Do NOT commit, revert, stash, or modify these. Stage commits with explicit file paths only (evaluator.ts, types.ts, tests/evaluator.test.ts, evaluator.py, tests/test_evaluator.py). If the full `pnpm test` run fails in cache.test.ts/client.test.ts due to those uncommitted user changes, treat it as pre-existing — verify via `npx vitest run tests/evaluator.test.ts` and report, do not "fix" the unrelated files.
</git_hygiene>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sdk-js AND mode (types + evaluator + tests)</name>
  <files>sdk/sdk-js/src/evaluator.ts, sdk/sdk-js/src/types.ts, sdk/sdk-js/tests/evaluator.test.ts</files>
  <behavior>
    New describe('rule_combination_mode AND (AND-01)') in tests/evaluator.test.ts (mirror the existing company-guard block style; helper entry: {enabled: true, scope: 'global', segments: [], default_val: false}):
    - and + all rules match => true: rules [{attribute:'country',operator:'equals',value:'PE',result:true},{attribute:'plan',operator:'equals',value:'pro',result:true}], user {country:'PE',plan:'pro'} => true
    - and + one rule fails => false: same rules, user {country:'PE',plan:'free'} => false
    - and + one rule fails => false even with default_val: true AND a manual segment containing the user (segments:[{id:1,type:'manual',conditions:[],members:['u1']}], user {sub:'u1',...}) — strict-false, segments/default not consulted
    - and + per-rule result ignored: both rules have result: false, all match => true
    - and + empty rules + default_val: true => true (legacy fall-through); and + empty rules + matching manual segment => true
    - legacy: rule_combination_mode undefined => first-match behavior (first matching rule's result returned) — e.g. rules [{...result:false},{...result:true}] where only the first matches => false
    - explicit 'first_match' behaves identically to undefined
    - and + rule with missing attribute => false (fail-closed)
  </behavior>
  <action>
    RED: write the describe block, run `npx vitest run tests/evaluator.test.ts`, confirm new tests fail (TS error on rule_combination_mode is acceptable RED signal — add the types.ts field together with the implementation in GREEN). GREEN: add `rule_combination_mode?: string | null` to FlagEntry in types.ts and the mode branch to evaluateFlag per the interfaces block; update the doc comment. Commit test(15-02) then feat(15-02) with explicit file paths.
  </action>
  <verify>
    <automated>cd sdk/sdk-js && npx vitest run tests/evaluator.test.ts && pnpm typecheck</automated>
  </verify>
  <done>All new AND cases pass; every pre-existing evaluator test green unmodified; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sdk-python AND mode (evaluator + tests)</name>
  <files>sdk/sdk-python/src/backoffice_sdk/evaluator.py, sdk/sdk-python/tests/test_evaluator.py</files>
  <behavior>
    New TestAndCombinationMode class in tests/test_evaluator.py mirroring Task 1's cases exactly in pytest form (entry dicts: {'enabled': True, 'scope': 'global', 'segments': [], 'default_val': False, 'rules': [...], 'rule_combination_mode': 'and'}):
    - all match => True; one fails => False; strict-false with default_val True + matching manual segment; per-rule result ignored (all result False, all match => True); empty rules fall-through (default_val True => True; matching segment => True); missing key => legacy first-match; explicit 'first_match' == missing; missing attribute in and mode => False.
  </behavior>
  <action>
    RED: write TestAndCombinationMode, run with the dedicated venv (`.venv/Scripts/python -m pytest` — [11-09] decision, do NOT use global python), confirm failures. GREEN: add the mode branch to evaluate_flag per the interfaces block (identical placement to sdk-js: after company guard, before first-match loop); update docstring. Commit test(15-02) then feat(15-02) with explicit file paths.
  </action>
  <verify>
    <automated>cd sdk/sdk-python && .venv/Scripts/python -m pytest tests/test_evaluator.py -x -q</automated>
  </verify>
  <done>sdk-python AND semantics matches sdk-js/backend case-for-case; full test_evaluator.py green.</done>
</task>

</tasks>

<verification>
- `cd sdk/sdk-js && npx vitest run tests/evaluator.test.ts && pnpm typecheck` green.
- `cd sdk/sdk-python && .venv/Scripts/python -m pytest tests/ -x -q` green.
- Diff check: OPERATORS dicts, evaluateRule/evaluate_rule, company guard, and segment blocks byte-identical to before (additive mode branch only).
</verification>

<success_criteria>
- Both SDK local evaluators implement AND mode with semantics identical to the backend spec (same test matrix passes in all three).
- Legacy bootstrap payloads without the field evaluate exactly as before (zero regression).
</success_criteria>

<output>
After completion, create `.planning/phases/15-and-rule-combination-semantics/15-02-SUMMARY.md`
</output>
