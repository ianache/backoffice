---
phase: 15-and-rule-combination-semantics
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - microuis/mui-feature-flags/src/composables/useRuleSimulator.ts
  - microuis/mui-feature-flags/src/composables/useRuleSimulator.test.ts
  - microuis/mui-feature-flags/src/components/flags/RuleSimulator.vue
  - microuis/mui-feature-flags/src/views/RuleBuilderView.vue
  - microuis/mui-feature-flags/src/services/flags.ts
autonomous: true
requirements: [AND-01, AND-02]

must_haves:
  truths:
    - "useRuleSimulator in 'and' mode exposes per-rule pass/fail results and one overall result that is true only when ALL rules pass"
    - "Live Simulator UI shows per-rule pass/fail rows plus an overall Passing/Failing badge in AND mode (no longer a single 'Matched Rule')"
    - "Rule Builder has a combination-mode selector (First match / All rules AND) persisted via PATCH rule_combination_mode on Save Changes"
    - "Legacy flags open with first_match selected and the simulator behaves exactly as before in first_match mode"
    - "Per-rule Result toggle is visibly communicated as ignored in AND mode"
  artifacts:
    - path: "microuis/mui-feature-flags/src/composables/useRuleSimulator.ts"
      provides: "mode param + ruleResults[] + overallResult refs"
      contains: "ruleResults"
    - path: "microuis/mui-feature-flags/src/components/flags/RuleSimulator.vue"
      provides: "combinationMode prop + per-rule pass/fail list + overall badge"
      contains: "combinationMode"
    - path: "microuis/mui-feature-flags/src/views/RuleBuilderView.vue"
      provides: "mode selector wired to store.updateFlag rule_combination_mode"
      contains: "rule_combination_mode"
    - path: "microuis/mui-feature-flags/src/services/flags.ts"
      provides: "rule_combination_mode on FeatureFlag + FlagPayload"
      contains: "rule_combination_mode"
  key_links:
    - from: "microuis/mui-feature-flags/src/views/RuleBuilderView.vue saveChanges"
      to: "PATCH /flags/{id} rule_combination_mode (backend Plan 15-01)"
      via: "store.updateFlag payload field"
      pattern: "rule_combination_mode"
    - from: "microuis/mui-feature-flags/src/components/flags/RuleSimulator.vue"
      to: "useRuleSimulator ruleResults/overallResult"
      via: "composable destructure + template list rendering"
      pattern: "ruleResults"
---

<objective>
Make the 4th evaluator (useRuleSimulator.ts) AND-aware and give the Rule Builder the mode UI: per-rule pass/fail + overall result in the Live Simulator (research Pitfall 3), a "Match: first rule / all rules (AND)" selector persisted to the backend, and the rule_combination_mode field on the MUI service types.

Purpose: completes AND-01 4-evaluator parity and the UI half of AND-02.
Output: Rule Builder can opt a flag into AND mode and simulate it correctly; legacy first_match UX unchanged.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-and-rule-combination-semantics/15-CONTEXT.md
@microuis/mui-feature-flags/src/composables/useRuleSimulator.ts
@microuis/mui-feature-flags/src/components/flags/RuleSimulator.vue
@microuis/mui-feature-flags/src/views/RuleBuilderView.vue
@microuis/mui-feature-flags/src/services/flags.ts
</context>

<interfaces>
LOCKED AND SEMANTICS — same spec as Plans 15-01/15-02: mode 'and' + non-empty rules => overall true only if ALL rules' conditions match; per-rule `result` ignored; empty rules => no verdict from rules (the real evaluators fall through to segments/default — the simulator simulates rules only, so overall is null). Legacy 'first_match' keeps first-match-wins with per-rule result.

New useRuleSimulator signature (3rd param; existing returns preserved so first_match consumers/tests keep working):
```typescript
export function useRuleSimulator(
  rules: Readonly<Ref<RuleSchema[]>>,
  contextJson: Ref<string>,
  mode: Readonly<Ref<string>> = ref('first_match') as Readonly<Ref<string>>,
) {
  // returns:
  // matchedIndex:  Ref<number | null>   — first_match only; always null in 'and' mode
  // matchedResult: Ref<boolean | null>  — first_match only; always null in 'and' mode
  // ruleResults:   Ref<boolean[]>       — per-rule evaluateRule outcome, BOTH modes, [] on context error
  // overallResult: Ref<boolean | null>  — 'and': rules.length ? ruleResults.every(Boolean) : null
  //                                     — 'first_match': matchedResult (null when no rule matched)
  // contextError:  Ref<string | null>   — unchanged
}
```
Inside the existing watchEffect: after parsing `user`, compute `ruleResults.value = ruleList.map((r) => evaluateRule(r, user))`; then branch on `mode.value` for matchedIndex/matchedResult/overallResult. evaluateRule and OPERATORS stay byte-identical (no operator changes this phase).

services/flags.ts additions:
- FeatureFlag: `rule_combination_mode: string` (backend normalizes — never null; values 'first_match' | 'and')
- FlagPayload: `rule_combination_mode?: string`
- FlagFilters/Segment/RuleSchema unchanged.

RuleSimulator.vue:
- New prop: `combinationMode?: string` (default 'first_match' via `props.combinationMode ?? 'first_match'` computed; pass `computed(() => props.combinationMode ?? 'first_match')` as the composable's mode param).
- Header badge: in 'and' mode drive Passing/Failing from `overallResult` (rules.length > 0); keep existing matchedResult-driven badge in first_match mode. Simplest: badge binds to `overallResult` in both modes since overallResult === matchedResult in first_match mode.
- Body: in first_match mode keep the existing "Matched Rule" panel untouched. In 'and' mode replace it with a "Rules (ALL must match)" list: one row per rule — "Rule {i+1}" + material-symbols icon `check_circle` (text-green-700) when ruleResults[i] else `cancel` (text-error), using inline Tailwind utilities (14-04 precedent — no new scoped styles). Below the list, when overallResult is false show muted text "Not all rules match — flag evaluates false"; keep the existing empty-state ("Add rules to start simulation") when rules.length === 0.

RuleBuilderView.vue:
- `const localMode = ref<'first_match' | 'and'>('first_match')`; in onMounted: `localMode.value = (flag.value.rule_combination_mode === 'and') ? 'and' : 'first_match'`.
- Selector UI: a white bar matching the Rollout slider styling (`flex items-center gap-md bg-white rounded-lg border border-outline-variant px-md py-md`, placed directly below the Rollout bar): label "Match" + two radio-style buttons or a native select with options "First matching rule (legacy)" / "All rules (AND)". When 'and' is selected show muted hint: "All rules must match — per-rule Result is ignored".
- AND connector label between RuleCards: replace the hardcoded `AND` text with `{{ localMode === 'and' ? 'AND' : 'ELSE IF' }}` so first_match chains stop reading as AND.
- saveChanges(): add `rule_combination_mode: localMode.value` to the store.updateFlag payload.
- Pass `:combination-mode="localMode"` to RuleSimulator.
</interfaces>

<git_hygiene>
The working tree contains UNRELATED uncommitted user changes (backend/app/domains/feature_flags/router.py, backend/app/domains/sdk/ws_router.py, bff/src/routes/sdk.ts, portal/src/main.ts, portal/src/composables/useBoFlags.ts, sdk/sdk-js/src/client.ts, sdk/sdk-js/tests/cache.test.ts, sdk/sdk-js/tests/client.test.ts, documentations/). Do NOT commit, revert, stash, or modify these. Stage commits with explicit file paths only.
</git_hygiene>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useRuleSimulator AND mode + ruleResults/overallResult</name>
  <files>microuis/mui-feature-flags/src/composables/useRuleSimulator.ts, microuis/mui-feature-flags/src/composables/useRuleSimulator.test.ts</files>
  <behavior>
    New describe('useRuleSimulator — AND combination mode') in useRuleSimulator.test.ts (use the composable directly with ref()s — no Vue mounting; await nextTick or use effectScope/watchEffect flush as the existing file's style dictates; rules helper: [{attribute:'country',operator:'equals',value:'PE',result:true},{attribute:'plan',operator:'equals',value:'pro',result:true}]):
    - and + all match: context {country:'PE',plan:'pro'} => ruleResults [true,true], overallResult true, matchedIndex null, matchedResult null
    - and + one fails: context {country:'PE',plan:'free'} => ruleResults [true,false], overallResult false
    - and + per-rule result ignored: rules with result:false but all matching => overallResult true
    - and + empty rules => ruleResults [], overallResult null
    - and + invalid context JSON => contextError set, ruleResults [], overallResult null
    - first_match (default 3rd param omitted): existing behavior — matchedIndex 0, matchedResult === rules[0].result, overallResult mirrors matchedResult, ruleResults populated
    - first_match + no match => matchedIndex null, overallResult null
  </behavior>
  <action>
    RED: write the new describe block, run `pnpm test`, confirm failures. GREEN: implement the new signature and returns per the interfaces block. Do NOT touch OPERATORS or evaluateRule. Keep all existing tests passing without modification. Commit test(15-03) then feat(15-03) with explicit file paths.
  </action>
  <verify>
    <automated>cd microuis/mui-feature-flags && pnpm test</automated>
  </verify>
  <done>New AND cases green; all pre-existing useRuleSimulator/evaluateRule tests green unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: RuleSimulator.vue per-rule pass/fail + overall badge</name>
  <files>microuis/mui-feature-flags/src/components/flags/RuleSimulator.vue</files>
  <action>
    Implement the RuleSimulator.vue changes from the interfaces block: combinationMode prop, mode param into useRuleSimulator (destructure ruleResults/overallResult too), badge driven by overallResult, AND-mode per-rule list with check_circle/cancel icons, first_match panel untouched, empty-state preserved. Keep the Test Context textarea, real-context toggle, and save-test-context emit exactly as-is (Phase 13 features must not regress).
  </action>
  <verify>
    <automated>cd microuis/mui-feature-flags && pnpm test && pnpm build</automated>
  </verify>
  <done>vite build green (vue-tsc not part of build — vitest + build are the gate); template renders per-rule rows only in 'and' mode; first_match rendering byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 3: RuleBuilderView mode selector + persistence + service types</name>
  <files>microuis/mui-feature-flags/src/views/RuleBuilderView.vue, microuis/mui-feature-flags/src/services/flags.ts</files>
  <action>
    1. services/flags.ts: add `rule_combination_mode: string` to FeatureFlag and `rule_combination_mode?: string` to FlagPayload (interfaces block).
    2. RuleBuilderView.vue: localMode ref + onMounted init, Match selector bar below the Rollout slider, dynamic AND/ELSE IF connector label, `:combination-mode="localMode"` on RuleSimulator, and `rule_combination_mode: localMode.value` added to the saveChanges() updateFlag payload (interfaces block). Leave the `complex: rules.length > 0` auto-set and test-context handler untouched.
  </action>
  <verify>
    <automated>cd microuis/mui-feature-flags && pnpm test && pnpm build</automated>
  </verify>
  <done>Selector defaults to first_match for legacy flags, reflects 'and' for AND flags, Save Changes PATCHes rule_combination_mode; build + tests green.</done>
</task>

</tasks>

<verification>
- `cd microuis/mui-feature-flags && pnpm test && pnpm build` green.
- grep checks: `rule_combination_mode` present in services/flags.ts, RuleBuilderView.vue; `ruleResults` in useRuleSimulator.ts and RuleSimulator.vue; `combinationMode` prop wired.
- Manual smoke (optional, requires stack running): open Rule Builder on a flag, switch Match to "All rules (AND)", simulator shows per-rule rows; Save Changes; reload — selector still shows AND.
</verification>

<success_criteria>
- useRuleSimulator implements the same AND truth table as the other 3 evaluators (per-rule fail-closed, result ignored, empty-rules null verdict).
- Simulator UI can express "N of M rules passed, overall false" (Pitfall 3 resolved).
- Mode round-trips: UI -> PATCH -> FlagResponse -> UI.
</success_criteria>

<output>
After completion, create `.planning/phases/15-and-rule-combination-semantics/15-03-SUMMARY.md`
</output>
