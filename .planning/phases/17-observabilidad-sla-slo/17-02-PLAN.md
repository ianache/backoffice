---
phase: 17-observabilidad-sla-slo
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - backend/app/domains/observability/router.py
  - backend/app/main.py
  - backend/tests/test_observability_router.py
  - bff/src/routes/observability.ts
  - bff/src/index.ts
autonomous: true
requirements: [OBS-03, OBS-04]
must_haves:
  truths:
    - "GET /observability/health/services returns the current UP/DEGRADED/DOWN status + latency for all monitored services and requires X-Internal-Secret"
    - "GET /observability/metrics?range=24h returns per-service uptime_pct, error_rate_pct, p95/p99 latency, and sample_count"
    - "The range query param only accepts 24h, 7d, or 30d and rejects arbitrary strings"
    - "tenant_id is accepted as a query param but does not filter results (documented Phase 17 limitation per D-05)"
    - "The BFF exposes /observability/* proxied to the backend, gated to PlatformAdmin/TenantOwner/TenantAdmin"
  artifacts:
    - path: "backend/app/domains/observability/router.py"
      provides: "GET /observability/health/services + GET /observability/metrics"
      contains: "prefix=\"/observability\""
    - path: "bff/src/routes/observability.ts"
      provides: "BFF proxy with requireAuth + requireRole + 5 forwarded headers"
      contains: "observabilityRouter"
    - path: "backend/tests/test_observability_router.py"
      provides: "ASGI integration tests for both endpoints"
      contains: "def test_health_services_endpoint"
  key_links:
    - from: "bff/src/routes/observability.ts"
      to: "backend /observability"
      via: "createProxyMiddleware pathRewrite"
      pattern: "/observability"
    - from: "backend/app/main.py"
      to: "observability_router"
      via: "include_router"
      pattern: "include_router\\(observability_router"
    - from: "backend/app/domains/observability/router.py"
      to: "service.list_current_status / service.aggregate_metrics"
      via: "handler delegation"
      pattern: "service\\.(list_current_status|aggregate_metrics)"
---

<objective>
Expose the observability data over HTTP: the backend router (GET /observability/health/services and GET /observability/metrics) mounted with the internal-secret guard, the FastAPI registration in main.py, the BFF proxy route (role-gated PlatformAdmin/TenantOwner/TenantAdmin), and the Wave 0 ASGI integration tests.

Purpose: Plans 03/04 (the dashboard) consume these two endpoints through the BFF. This plan turns the collected samples (Plan 01) into queryable read endpoints with the project's standard auth chain.
Output: Two authenticated read endpoints reachable as GET /observability/health/services and GET /observability/metrics through the BFF, with passing integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observabilidad-sla-slo/17-RESEARCH.md
@.planning/phases/17-observabilidad-sla-slo/17-PATTERNS.md
@.planning/phases/17-observabilidad-sla-slo/17-CONTEXT.md
@.planning/phases/17-observabilidad-sla-slo/17-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (already implemented) -->
backend/app/domains/observability/service.py:
  async def list_current_status(db) -> list[ServiceHealthSample]   # latest row per service_name
  async def aggregate_metrics(db, service_name: str, range_key: str) -> dict  # uptime_pct, error_rate_pct, p95/p99, sample_count
backend/app/domains/observability/schemas.py:
  ServiceHealthListResponse(items: List[ServiceHealthResponse])
  MetricsResponse(items: List[ServiceMetrics], range: str)
Service names: fastapi, mysql, bff, keycloak, ws_gateway

<!-- Existing deps to reuse -->
from app.dependencies import get_db, verify_internal_secret
APIRouter(prefix="/observability", tags=["observability"], dependencies=[Depends(verify_internal_secret)])

<!-- BFF building blocks -->
bff/src/routes/labels.ts: requireAuth + requireRole(...) + createProxyMiddleware + 5 forwarded headers
bff/src/middleware/roles.ts: requireRole(...roles)
config.backendUrl, config.internalSecret
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Observability router + main.py registration + ASGI integration tests</name>
  <files>backend/app/domains/observability/router.py, backend/app/main.py, backend/tests/test_observability_router.py</files>
  <read_first>
    - backend/app/domains/audit/router.py (APIRouter prefix + dependencies=[Depends(verify_internal_secret)], Query/Header param patterns)
    - backend/app/domains/observability/service.py (list_current_status, aggregate_metrics signatures from Plan 01)
    - backend/app/domains/observability/schemas.py (ServiceHealthListResponse, MetricsResponse, ServiceMetrics)
    - backend/app/main.py (current include_router block + ConnectionManager ordering + lifespan added in Plan 01)
    - backend/app/dependencies.py (get_db, verify_internal_secret)
    - backend/tests/test_labels_router.py (TestClient + dependency_overrides for get_db and verify_internal_secret)
    - .planning/phases/17-observabilidad-sla-slo/17-PATTERNS.md (router.py and test_observability_router.py pattern sections)
  </read_first>
  <behavior>
    - test_health_services_endpoint: seed ServiceHealthSample rows for several services; GET /observability/health/services returns one item per service with status + latency_ms; without overridden verify_internal_secret it would be gated (override applied in fixture, as in test_labels_router.py)
    - test_metrics_endpoint_ranges: seed rows across timestamps; GET /observability/metrics?range=24h returns items with uptime_pct, error_rate_pct, p95_latency_ms, p99_latency_ms, sample_count and range=="24h"
    - test_metrics_rejects_bad_range: GET /observability/metrics?range=99y returns 422 (range restricted to 24h|7d|30d)
    - test_metrics_accepts_tenant_id_unfiltered: GET /observability/metrics?range=24h&tenant_id=5 returns 200 with the same aggregates (tenant_id accepted, not filtered — D-05)
  </behavior>
  <action>
    Create router.py: `router = APIRouter(prefix="/observability", tags=["observability"], dependencies=[Depends(verify_internal_secret)])` (mirror audit/router.py). Define two handlers:
    - GET "/health/services" returning ServiceHealthListResponse — call service.list_current_status(db), wrap rows into ServiceHealthResponse items.
    - GET "/metrics" returning MetricsResponse — accept `range: Literal["24h","7d","30d"] = Query("24h")` (rejects arbitrary strings → 422, Security Domain V5) and `tenant_id: Optional[str] = Query(None)` (accepted, NOT used in any WHERE clause — add an inline comment "D-05: accepted-not-filtered, true per-tenant metrics arrive in Phase 18"). For each of the 5 service names call service.aggregate_metrics(db, name, range) and assemble MetricsResponse(items=[...], range=range). Use `db: AsyncSession = Depends(get_db)` per handler. Backend router does NOT branch on roles — read vs full access is enforced at the BFF (D-10/D-11; Phase 17 has only GETs).

    Edit main.py: add `from app.domains.observability.router import router as observability_router` to the imports and `app.include_router(observability_router)` alongside the existing include_router calls (no /api/v1 prefix — mirrors audit_router, so the final mounted paths are /observability/health/services and /observability/metrics). Do not touch the lifespan or /health liveness endpoint added/kept in Plan 01.

    Create backend/tests/test_observability_router.py implementing the four behaviors. Copy the db_session + client fixtures from test_labels_router.py verbatim (in-memory SQLite, dependency_overrides for get_db and verify_internal_secret, INTERNAL_HEADERS). Seed ServiceHealthSample rows via db_session.add(...) + commit before each client.get(...). Use the repo asyncio_mode convention.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_observability_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_observability_router.py -x` passes (health endpoint, metrics ranges, bad-range 422, tenant_id-unfiltered)
    - router.py declares APIRouter(prefix="/observability", ...) with dependencies=[Depends(verify_internal_secret)]
    - GET /observability/metrics with range not in {24h,7d,30d} returns HTTP 422
    - main.py contains `app.include_router(observability_router)`
    - GET /observability/health/services returns ServiceHealthListResponse-shaped JSON (items with service_name/status/latency_ms)
  </acceptance_criteria>
  <done>Both read endpoints exist, are mounted, internal-secret gated, range-validated, and covered by green integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: BFF observability proxy route + index.ts registration</name>
  <files>bff/src/routes/observability.ts, bff/src/index.ts</files>
  <read_first>
    - bff/src/routes/labels.ts (requireAuth + requireRole + createProxyMiddleware + pathRewrite + 5 forwarded headers — closest role-gated analog)
    - bff/src/routes/audit.ts (header-forwarding proxy shape)
    - bff/src/middleware/roles.ts (requireRole signature)
    - bff/src/middleware/auth.ts (requireAuth)
    - bff/src/config/index.ts (config.backendUrl, config.internalSecret)
    - bff/src/index.ts (current router imports + app.use mounts + the existing app.get('/health') liveness route at line ~22)
  </read_first>
  <action>
    Create bff/src/routes/observability.ts exporting `observabilityRouter`. Mirror labels.ts: `observabilityRouter.use(requireAuth, requireRole('PlatformAdmin','TenantOwner','TenantAdmin'), createProxyMiddleware({ target: config.backendUrl, changeOrigin: true, pathRewrite: (path) => `/observability${path}`, on: { proxyReq: ... } }))`. The proxyReq handler must set the same 5 headers as labels.ts: X-Internal-Secret (config.internalSecret), X-User-Sub, X-User-Email, X-User-Roles (joined with ','), X-User-Tenant-Id. Use `.js` import extensions to match existing ESM imports.

    Edit bff/src/index.ts: add `import { observabilityRouter } from './routes/observability.js'` with the other route imports, and `app.use('/observability', observabilityRouter)` alongside the other app.use mounts (NO express.json() — proxy streams the raw body, matching the audit/labels comment convention). Add the standard explanatory comment line above the mount. Do NOT alter the existing top-level `app.get('/health', ...)` liveness route — the new mount is at /observability, which does not collide with /health.
  </action>
  <verify>
    <automated>cd bff && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - bff/src/routes/observability.ts exports observabilityRouter using requireAuth + requireRole('PlatformAdmin','TenantOwner','TenantAdmin') + createProxyMiddleware with pathRewrite adding the /observability prefix
    - observabilityRouter proxyReq sets X-Internal-Secret, X-User-Sub, X-User-Email, X-User-Roles, X-User-Tenant-Id
    - bff/src/index.ts imports observabilityRouter and mounts it via app.use('/observability', observabilityRouter)
    - `pnpm exec tsc --noEmit` (in bff/) passes with no new type errors
    - The existing app.get('/health', ...) liveness route in index.ts is unchanged
  </acceptance_criteria>
  <done>BFF exposes /observability/* proxied to the backend with the standard auth + role gate and forwarded headers; BFF typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → BFF | Authenticated portal user requests observability data |
| BFF → backend | BFF forwards with X-Internal-Secret + identity headers |
| query string → backend | range/tenant_id params cross into DB query layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation of Privilege | /observability endpoints | mitigate | BFF requireRole('PlatformAdmin','TenantOwner','TenantAdmin') gates access; backend dependencies=[Depends(verify_internal_secret)] blocks direct external calls (V4) |
| T-17-05 | Tampering / Injection | range query param | mitigate | range typed as Literal["24h","7d","30d"] → FastAPI returns 422 on anything else (V5) |
| T-17-06 | Info Disclosure | tenant_id echo | mitigate | tenant_id accepted but never interpolated into SQL or echoed into details; treated as opaque string (D-05) |
| T-17-SC | Tampering | npm/pip installs | accept | No new packages — http-proxy-middleware, express middleware all pre-existing — no legitimacy gate needed |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_observability_router.py -x` is green
- `cd bff && pnpm exec tsc --noEmit` passes
- `cd backend && python -m pytest` full backend suite green (wave merge gate)
</verification>

<success_criteria>
- GET /observability/health/services returns current per-service status, internal-secret gated
- GET /observability/metrics?range=24h|7d|30d returns per-service aggregates; bad range → 422; tenant_id accepted-not-filtered
- BFF /observability/* proxied with PlatformAdmin/TenantOwner/TenantAdmin gate + 5 forwarded headers
- Backend and BFF both build/typecheck clean
</success_criteria>

<output>
Create `.planning/phases/17-observabilidad-sla-slo/17-02-SUMMARY.md` when done
</output>
