---
phase: 17-observabilidad-sla-slo
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - microuis/mui-observability/package.json
  - microuis/mui-observability/vite.config.ts
  - microuis/mui-observability/tsconfig.json
  - microuis/mui-observability/tailwind.config.js
  - microuis/mui-observability/postcss.config.js
  - microuis/mui-observability/index.html
  - microuis/mui-observability/src/routes.ts
  - microuis/mui-observability/src/components/observability/StatusCard.vue
  - microuis/mui-observability/src/components/observability/UptimeSummary.vue
  - microuis/mui-observability/src/components/observability/RangeSelector.vue
  - microuis/mui-observability/src/components/observability/LatencyTrendChart.vue
  - microuis/mui-observability/src/components/observability/StatusCard.spec.ts
  - microuis/mui-observability/src/components/observability/UptimeSummary.spec.ts
  - microuis/mui-observability/src/components/observability/RangeSelector.spec.ts
autonomous: true
requirements: [OBS-08]
user_setup:
  - service: chart.js + vue-chartjs
    why: "Latency trend line chart rendering (vetted [OK] in 17-RESEARCH.md Package Legitimacy Audit)"
    env_vars: []
must_haves:
  truths:
    - "A mui-observability workspace package exists with chart.js + vue-chartjs installed, port 5180, and the same shared Vue/Pinia/Router/Axios singletons as mui-labeling"
    - "StatusCard renders the component name, a UP/DEGRADED/DOWN badge (green/amber/red), and the current latency (or — when null/DOWN)"
    - "UptimeSummary renders uptime % with green/amber/red thresholds plus p95/p99 and error rate, with an SLO breach indicator when p95 > 100ms"
    - "RangeSelector renders a 24h/7d/30d segmented control with 24h active by default and emits the selected range"
    - "LatencyTrendChart registers only the chart.js modules needed for a Line chart (tree-shaken, never chart.js/auto) and renders a time-series from labels + per-service datasets"
  artifacts:
    - path: "microuis/mui-observability/package.json"
      provides: "@backoffice/mui-observability scaffold with chart.js + vue-chartjs"
      contains: "@backoffice/mui-observability"
    - path: "microuis/mui-observability/vite.config.ts"
      provides: "Module Federation config, name mui-observability, port 5180, ./routes expose"
      contains: "mui-observability"
    - path: "microuis/mui-observability/src/components/observability/StatusCard.vue"
      provides: "Per-service status card with 3-state chip CSS"
      contains: "status-up"
    - path: "microuis/mui-observability/src/components/observability/LatencyTrendChart.vue"
      provides: "chart.js Line wrapper, tree-shaken registration"
      contains: "ChartJS.register"
  key_links:
    - from: "microuis/mui-observability/src/components/observability/LatencyTrendChart.vue"
      to: "chart.js + vue-chartjs"
      via: "tree-shaken ChartJS.register + Line component"
      pattern: "ChartJS\\.register"
    - from: "microuis/mui-observability/src/routes.ts"
      to: "ObservabilityView"
      via: "lazy route component import"
      pattern: "ObservabilityView"
---

<objective>
Scaffold the new federated micro-UI mui-observability (port 5180, cloned from mui-labeling) and build its four leaf components — StatusCard, UptimeSummary, RangeSelector, LatencyTrendChart — with vitest component tests, following the 17-UI-SPEC.md design contract exactly. Install chart.js + vue-chartjs (vetted [OK]).

Purpose: These are the reusable presentation pieces the dashboard view (Plan 04) composes. Building and testing them before the view (interface-first) means Plan 04 only wires data and registers the remote.
Output: A buildable mui-observability package with four tested components and a routes.ts pointing at ObservabilityView (created in Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observabilidad-sla-slo/17-UI-SPEC.md
@.planning/phases/17-observabilidad-sla-slo/17-PATTERNS.md
@.planning/phases/17-observabilidad-sla-slo/17-RESEARCH.md
@.planning/phases/17-observabilidad-sla-slo/17-CONTEXT.md

<interfaces>
<!-- Component prop/event contracts (consumed by ObservabilityView in Plan 04) -->
StatusCard props: { name: string; status: 'UP'|'DEGRADED'|'DOWN'; latencyMs: number | null }
UptimeSummary props: { name: string; uptimePct: number; errorRatePct: number; p95LatencyMs: number | null; p99LatencyMs: number | null }
RangeSelector props: { modelValue: '24h'|'7d'|'30d' }  emits: 'update:modelValue' with the selected range
LatencyTrendChart props: { datasets: Array<{ label: string; data: Array<number | null>; borderColor: string }>; labels: string[] }  // 5 datasets, one per service; data is the per-time-bucket avg latency (null = missing bucket so chart.js gaps the line). This is a pure presentational time-series chart — the parent (Plan 04 ObservabilityView) shapes labels+datasets from the metrics `history` series; this component does NOT fetch or bucket.

<!-- Service display names (title-cased) for the dashboard, mapped from backend service_name -->
fastapi -> "FastAPI Core", bff -> "BFF", mysql -> "MySQL", keycloak -> "Keycloak", ws_gateway -> "WebSocket Gateway"

<!-- Status -> chip class -> color (reuse TenantTable.vue values verbatim, rename classes) -->
UP -> .status-up    : light #dcfce7/#166534, dark rgba(52,168,83,0.15)/#86efac
DEGRADED -> .status-degraded : light #fef3c7/#92400e, dark rgba(245,158,11,0.15)/#fcd34d
DOWN -> .status-down : light #fee2e2/#991b1b, dark rgba(239,68,68,0.15)/#fca5a5

<!-- Frontend route guard (mirrors mui-labeling/src/routes.ts) -->
roles: ['PlatformAdmin', 'TenantOwner', 'TenantAdmin']  (D-10/D-11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold mui-observability package (clone of mui-labeling, port 5180) + install chart deps</name>
  <files>microuis/mui-observability/package.json, microuis/mui-observability/vite.config.ts, microuis/mui-observability/tsconfig.json, microuis/mui-observability/tailwind.config.js, microuis/mui-observability/postcss.config.js, microuis/mui-observability/index.html, microuis/mui-observability/src/routes.ts</files>
  <read_first>
    - microuis/mui-labeling/package.json (scripts, deps, devDeps — clone this)
    - microuis/mui-labeling/vite.config.ts (federation name/port/exposes/shared singletons)
    - microuis/mui-labeling/tsconfig.json (clone verbatim)
    - microuis/mui-labeling/tailwind.config.js (token mapping — copy verbatim per UI-SPEC)
    - microuis/mui-labeling/postcss.config.js (clone verbatim)
    - microuis/mui-labeling/index.html (clone, retitle)
    - microuis/mui-labeling/src/routes.ts (route record shape, meta.roles guard)
  </read_first>
  <action>
    Create microuis/mui-observability/ by cloning the mui-labeling scaffold with these concrete substitutions:
    - package.json: name "@backoffice/mui-observability"; in scripts change prepreview/preview port 5179 → 5180; DROP vuedraggable from dependencies (not used); ADD "chart.js": "^4.5.1" and "vue-chartjs": "^5.3.3" to dependencies. Keep axios/pinia/vue/vue-router and the mui-labeling devDependencies (federation 1.4.1, plugin-vue, tailwind, vitest ^1.6.0, vite ^5.3.1, etc.).
    - vite.config.ts: change `base` and `preview.port` 5179 → 5180; federation name 'mui-labeling' → 'mui-observability'; keep `exposes: { './routes': './src/routes.ts' }`, keep `remotes.shell` pointing at http://localhost:5173/assets/remoteEntry.js, and keep the IDENTICAL shared singleton block (vue/pinia/vue-router/axios, singleton:true, requiredVersion:false). Do NOT add chart.js/vue-chartjs to shared — bundle per-remote.
    - tsconfig.json, postcss.config.js, tailwind.config.js: copy verbatim from mui-labeling (tailwind.config.js is the token source of truth per UI-SPEC — do not edit).
    - index.html: clone, retitle to "Observability".
    - src/routes.ts: a single RouteRecordRaw with path '/observability', name 'observability', component () => import('./views/ObservabilityView.vue') (the view file is created in Plan 04; the import is lazy so the build of components in this plan does not require it, but note Plan 04 must land before a full Shell load). meta: { requiresAuth: true, layout: 'main', title: 'Observability', roles: ['PlatformAdmin','TenantOwner','TenantAdmin'] }.

    Install deps: run `pnpm install --filter @backoffice/mui-observability` (or `pnpm install` at workspace root) so chart.js@^4.5.1 and vue-chartjs@^5.3.3 are linked from the workspace store. These two packages are vetted [OK] (no postinstall, RESEARCH.md Package Legitimacy Audit) — no legitimacy checkpoint needed.
  </action>
  <verify>
    <automated>cd microuis/mui-observability && node -e "const p=require('./package.json'); if(p.name!=='@backoffice/mui-observability') throw new Error('name'); if(!p.dependencies['chart.js']||!p.dependencies['vue-chartjs']) throw new Error('chart deps'); if(p.dependencies['vuedraggable']) throw new Error('vuedraggable should be removed'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - microuis/mui-observability/package.json name is "@backoffice/mui-observability" and lists chart.js ^4.5.1 + vue-chartjs ^5.3.3, with vuedraggable removed
    - vite.config.ts federation name is 'mui-observability', base/preview port is 5180, exposes './routes', and the shared block lists vue/pinia/vue-router/axios as singletons
    - chart.js and vue-chartjs are NOT present in the vite.config.ts shared block
    - src/routes.ts exports a route with path '/observability', meta.roles ['PlatformAdmin','TenantOwner','TenantAdmin'], lazy-importing ObservabilityView.vue
    - chart.js and vue-chartjs resolve (node -e require succeeds or pnpm list shows them installed)
  </acceptance_criteria>
  <done>mui-observability scaffold exists on port 5180 with chart deps installed and routes.ts pointing at the (Plan 04) view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusCard, UptimeSummary, RangeSelector components + vitest specs</name>
  <files>microuis/mui-observability/src/components/observability/StatusCard.vue, microuis/mui-observability/src/components/observability/UptimeSummary.vue, microuis/mui-observability/src/components/observability/RangeSelector.vue, microuis/mui-observability/src/components/observability/StatusCard.spec.ts, microuis/mui-observability/src/components/observability/UptimeSummary.spec.ts, microuis/mui-observability/src/components/observability/RangeSelector.spec.ts</files>
  <read_first>
    - .planning/phases/17-observabilidad-sla-slo/17-UI-SPEC.md (Status card anatomy, Uptime summary card anatomy, Range selector anatomy, Color status table, Copywriting contract)
    - microuis/mui-tenants/src/components/tenants/TenantTable.vue (lines ~254-293: .status-chip/.status-active/.status-pending/.status-suspended/.status-dot CSS + [data-theme='dark'] overrides to copy and rename)
    - microuis/mui-labeling/src/views/LabelingView.vue (header/toggle button style for the segmented range control)
    - microuis/mui-labeling (any existing *.spec.ts / vitest usage to mirror @vue/test-utils mount conventions; if none, follow mui-feature-flags spec conventions noted in STATE.md)
  </read_first>
  <behavior>
    - StatusCard: given { name:'FastAPI Core', status:'UP', latencyMs:42 } renders "FastAPI Core", a badge with text "UP" carrying class status-up, and "42 ms". Given status:'DOWN', latencyMs:null renders the "DOWN" badge with class status-down and shows "—" (not "null ms"). Given status:'DEGRADED' renders class status-degraded and badge text "DEGRADED".
    - UptimeSummary: given uptimePct 99.95 hero metric shows "99.9%"/"100.0%" formatting (one decimal) with the green tier; uptimePct 99.5 → amber tier; uptimePct 98.0 → red tier. Renders "p95: {n}ms", "p99: {n}ms", "error rate: {n}%". When p95LatencyMs > 100 renders an SLO-breach indicator (a material-symbols "warning" element / element with a breach class or tooltip text "SLO breach").
    - RangeSelector: renders three segments "24h", "7d", "30d"; the segment matching modelValue carries the active class (bg-primary text-on-primary); clicking "7d" emits update:modelValue with "7d".
  </behavior>
  <action>
    Create StatusCard.vue (props per interfaces). Card shell `bg-surface-container-lowest border border-outline-variant rounded-xl p-md shadow-sm`. Header row: name (`text-title-md font-bold`) left, status badge right. Badge reuses the chip pattern — copy the .status-chip/.status-dot scoped CSS block AND the three color classes + their `[data-theme='dark']` overrides from TenantTable.vue, renaming to `.status-up`/`.status-degraded`/`.status-down` with the IDENTICAL color values (UP→green, DEGRADED→amber, DOWN→red). Hero metric: latency value `text-headline-lg font-mono tabular-nums` with caption "current latency" (`text-label-sm text-on-surface-variant uppercase`); when latencyMs is null render "—" and caption "unreachable". DOWN state: card border `border-error/40`. DEGRADED: hero metric text uses the amber `.status-pending`-equivalent color.

    Create UptimeSummary.vue (props per interfaces). Same card shell as StatusCard. Name (`text-title-md font-bold`); hero uptime % `text-headline-lg font-mono tabular-nums` formatted to one decimal with color tiers: green if >=99.9, amber if 99.0–99.89, red if <99.0 (reuse the same 3 status text colors). Caption "uptime". Divider, then secondary stats row `text-body-md font-mono`: "p95: {n}ms   p99: {n}ms" and "error rate: {n}%". If p95LatencyMs > 100, render a `material-symbols-outlined` "warning" icon `text-error text-[16px]` next to p95 with title/tooltip "SLO breach".

    Create RangeSelector.vue (modelValue + update:modelValue per interfaces). Pill segmented control: container `inline-flex items-center gap-xs p-xs bg-surface-container rounded-full`; active segment `bg-primary text-on-primary font-bold rounded-full px-md py-1`; inactive `text-on-surface-variant hover:bg-surface-container-high rounded-full px-md py-1`. Segments "24h","7d","30d"; clicking emits update:modelValue.

    Create the three .spec.ts files using @vue/test-utils + vitest, asserting the behaviors above (mount with props, query rendered text/classes, trigger click and assert emitted events). Follow the mui-labeling/mui-feature-flags vitest spec conventions.
  </action>
  <verify>
    <automated>cd microuis/mui-observability && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` (in microuis/mui-observability) passes all three spec files
    - StatusCard.vue renders status-up/status-degraded/status-down classes by status and shows "—" when latencyMs is null
    - UptimeSummary.vue applies green/amber/red uptime tiers and renders an SLO-breach indicator when p95 > 100
    - RangeSelector.vue emits update:modelValue with the clicked range and marks the active segment with bg-primary
    - The .status-up/.status-degraded/.status-down color values match TenantTable.vue's status-active/status-pending/status-suspended verbatim (no new colors)
  </acceptance_criteria>
  <done>Three presentation components implemented to the UI-SPEC and covered by green vitest specs.</done>
</task>

<task type="auto">
  <name>Task 3: LatencyTrendChart (chart.js Line, tree-shaken) + render smoke spec</name>
  <files>microuis/mui-observability/src/components/observability/LatencyTrendChart.vue</files>
  <read_first>
    - .planning/phases/17-observabilidad-sla-slo/17-RESEARCH.md (Pattern 4: tree-shaken ChartJS.register + Line; Anti-Patterns: never chart.js/auto)
    - .planning/phases/17-observabilidad-sla-slo/17-UI-SPEC.md (Latency Trend section: 5 datasets, time-bucketed X-axis from the metrics history series, chart color palette per service, SLO breach dashed reference line, min-h-[280px])
    - microuis/mui-observability/src/components/observability/StatusCard.spec.ts (vitest mount conventions established in Task 2)
  </read_first>
  <action>
    Create LatencyTrendChart.vue (props: datasets, labels per interfaces — datasets[].data is Array<number | null> so missing time buckets gap the line). Import only the chart.js modules needed for a line chart and register them via ChartJS.register(Title, Tooltip, Legend, LineElement, LinearScale, CategoryScale, PointElement) — copy RESEARCH.md Pattern 4. NEVER import chart.js/auto. Render `<Line :data="chartData" :options="chartOptions" />` from vue-chartjs inside a container with `min-h-[280px]`. Build chartData from props (labels = the time-bucket x-axis from the parent; datasets each already carrying its borderColor from the parent per the UI-SPEC service color palette). Set `spanGaps: false` (or leave default) so null points break the line at missing buckets. chartOptions: responsive true, maintainAspectRatio false, legend position 'top', y-axis title "latency_ms". Add the SLO threshold as a lightweight dashed reference dataset (a flat array at value 100 across all labels with borderDash:[5,5] and the error color at ~50% opacity) — do NOT add chartjs-plugin-annotation.

    Keep this component dumb (presentational): no fetching, no bucketing — it receives labels+datasets already shaped by ObservabilityView (Plan 04). Add a minimal render smoke check to one of the existing spec files OR a new LatencyTrendChart.spec.ts that mounts it with one dataset + labels and asserts it renders without throwing (stub/shallow-mount the Line component if chart.js canvas rendering is unavailable in jsdom — mock 'vue-chartjs' to a stub component so the test asserts props flow, not canvas pixels).
  </action>
  <verify>
    <automated>cd microuis/mui-observability && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - LatencyTrendChart.vue calls ChartJS.register(...) with explicit modules and does NOT import 'chart.js/auto'
    - The component renders the vue-chartjs Line with data derived from the datasets/labels props inside a min-h-[280px] container
    - datasets[].data accepts null entries (gapped buckets) and the component does not coerce them to 0
    - A dashed 100ms SLO reference line is present in chartData (borderDash) without the chartjs-plugin-annotation package
    - `pnpm test` still passes (including a mount/smoke assertion for LatencyTrendChart)
  </acceptance_criteria>
  <done>LatencyTrendChart renders a tree-shaken chart.js Line over the time-bucketed labels with the SLO threshold line and passes its smoke spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | New chart.js + vue-chartjs packages added to the monorepo |
| federation host → remote | Shell loads mui-observability remoteEntry at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-SC | Tampering | chart.js + vue-chartjs npm installs | mitigate | Both vetted [OK] via slopcheck with no postinstall scripts (17-RESEARCH.md Package Legitimacy Audit, verified 2026-06-13); pinned at ^4.5.1 / ^5.3.3 — no [ASSUMED]/[SUS] packages, no blocking checkpoint required |
| T-17-07 | Information Disclosure | StatusCard details rendering | accept | Components render only status/latency/uptime numbers passed as props; raw error `details` strings are not surfaced in the UI in Phase 17 |
| T-17-08 | DoS (bundle bloat) | per-remote chart.js bundle | accept | Tree-shaken registration (Pattern 4) keeps the bundle minimal; chart.js not added to federation shared singletons by design |
</threat_model>

<verification>
- `cd microuis/mui-observability && pnpm test` is green (StatusCard, UptimeSummary, RangeSelector, LatencyTrendChart specs)
- package.json has chart.js + vue-chartjs and no vuedraggable
- LatencyTrendChart uses tree-shaken registration (no chart.js/auto)
</verification>

<success_criteria>
- mui-observability scaffold exists on port 5180 mirroring mui-labeling singletons
- StatusCard / UptimeSummary / RangeSelector / LatencyTrendChart implemented to 17-UI-SPEC.md and tested
- Status colors reuse TenantTable.vue values verbatim; no new brand colors
- chart.js Line is tree-shaken over a time-bucketed x-axis; SLO threshold line present without extra plugin
</success_criteria>

<output>
Create `.planning/phases/17-observabilidad-sla-slo/17-03-SUMMARY.md` when done
</output>
</content>
