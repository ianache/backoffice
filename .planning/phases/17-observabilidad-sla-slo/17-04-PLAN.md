---
phase: 17-observabilidad-sla-slo
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - microuis/mui-observability/src/views/ObservabilityView.vue
  - microuis/mui-observability/src/services/observability.ts
  - portal/vite.config.ts
  - portal/src/router/index.ts
  - portal/.env.example
  - portal/src/components/layout/MainLayout.vue
autonomous: false
requirements: [OBS-07, OBS-08]
must_haves:
  truths:
    - "ObservabilityView composes the page header, RangeSelector, 5 StatusCards, the LatencyTrendChart, and 5 UptimeSummary cards from real /observability data via the BFF"
    - "The LatencyTrendChart is fed from the metrics `history` series — one dataset per service built from each service's history points, with labels derived from the union of bucket timestamps for the selected range"
    - "Status cards re-poll GET /observability/health/services every 15s and clear the interval on unmount; metrics re-fetch only when the range changes"
    - "The Shell registers mui-observability in portal/vite.config.ts, REMOTE_MANIFEST, the importRemote switch, and .env.example on port 5180"
    - "MainLayout shows an 'Observability' nav item (icon monitor_heart) only for PlatformAdmin/TenantOwner/TenantAdmin, with the breadcrumb label wired"
    - "mui-observability builds and exposes ./routes; the dashboard loads in the Shell behind the role-gated nav item"
  artifacts:
    - path: "microuis/mui-observability/src/views/ObservabilityView.vue"
      provides: "Dashboard page composing all components + data fetch + auto-refresh + history->chart mapping"
      contains: "RangeSelector"
    - path: "microuis/mui-observability/src/services/observability.ts"
      provides: "fetchServices() + fetchMetrics(range) BFF clients"
      contains: "/observability"
    - path: "portal/src/router/index.ts"
      provides: "REMOTE_MANIFEST + importRemote case for mui-observability"
      contains: "mui-observability"
    - path: "portal/src/components/layout/MainLayout.vue"
      provides: "Observability nav item + breadcrumb label"
      contains: "monitor_heart"
  key_links:
    - from: "ObservabilityView.vue"
      to: "/observability/health/services + /observability/metrics"
      via: "axios calls through the BFF (services/observability.ts)"
      pattern: "/observability"
    - from: "ObservabilityView.vue"
      to: "LatencyTrendChart datasets/labels"
      via: "map metrics.items[].history -> chart datasets + labels"
      pattern: "history"
    - from: "portal/src/router/index.ts"
      to: "mui-observability/routes"
      via: "importRemote switch case"
      pattern: "import\\('mui-observability/routes'\\)"
    - from: "portal/vite.config.ts"
      to: "VITE_REMOTE_OBSERVABILITY"
      via: "remotes['mui-observability'] entry (port 5180)"
      pattern: "mui-observability"
---

<objective>
Compose the dashboard view (ObservabilityView) from the Plan 03 components with real data + auto-refresh, and register mui-observability as a Shell remote (portal vite.config, REMOTE_MANIFEST, importRemote switch, .env.example) plus the role-gated nav item and breadcrumb in MainLayout. End with a build smoke check and a human visual/role verification.

Purpose: This is the final wiring that makes Phase 17 user-visible — turning the tested components and backend endpoints into a working dashboard reachable from the Shell nav for the authorized roles.
Output: A federated Observability dashboard loadable in the portal for PlatformAdmin/TenantOwner/TenantAdmin, showing live service status, latency trends, and uptime SLOs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-observabilidad-sla-slo/17-UI-SPEC.md
@.planning/phases/17-observabilidad-sla-slo/17-PATTERNS.md
@.planning/phases/17-observabilidad-sla-slo/17-CONTEXT.md
@.planning/phases/17-observabilidad-sla-slo/17-03-SUMMARY.md

<interfaces>
<!-- Component contracts from Plan 03 -->
StatusCard props: { name, status:'UP'|'DEGRADED'|'DOWN', latencyMs:number|null }
UptimeSummary props: { name, uptimePct, errorRatePct, p95LatencyMs:number|null, p99LatencyMs:number|null }
RangeSelector: v-model -> '24h'|'7d'|'30d' (default 24h)
LatencyTrendChart props: { datasets:[{label,data:number[],borderColor}], labels:string[] }

<!-- Backend responses (through BFF /observability) -->
GET /observability/health/services -> { items: [{ id, checked_at, service_name, status, latency_ms, details }] }   // one per service
GET /observability/metrics?range=24h -> {
  items: [{
    service_name, uptime_pct, error_rate_pct, p95_latency_ms, p99_latency_ms, sample_count,
    history: [{ ts: string, avg_latency_ms: number|null }]   // ordered ascending; hourly buckets for 24h, daily for 7d/30d
  }],
  range
}

<!-- service_name -> display name + chart color (UI-SPEC) -->
fastapi->"FastAPI Core" #a9000b/#abc7ff | bff->"BFF" #00529d/#67df70 | mysql->"MySQL" #16a34a/#4ade80 | keycloak->"Keycloak" #f59e0b/#fcd34d | ws_gateway->"WebSocket Gateway" #5f5e5e/#a2c9ff

<!-- Existing Shell registration patterns (mirror mui-labeling exactly) -->
portal/vite.config.ts line ~28: remotes['mui-labeling'] = env.VITE_REMOTE_LABELING ? ... : 'http://localhost:5179/assets/remoteEntry.js'
portal/src/router/index.ts line 29: REMOTE_MANIFEST entry; lines 73-75: importRemote case 'mui-labeling'
portal/.env.example: VITE_REMOTE_LABELING=http://localhost:5179
portal/src/components/layout/MainLayout.vue: Audit Log nav button (~240-255) + breadcrumb label (~311); auth via authStore.hasRole(...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ObservabilityView + observability service client (data fetch, mapping, auto-refresh)</name>
  <files>microuis/mui-observability/src/views/ObservabilityView.vue, microuis/mui-observability/src/services/observability.ts</files>
  <read_first>
    - .planning/phases/17-observabilidad-sla-slo/17-UI-SPEC.md (Page structure, Component inventory, Loading/Empty/Error states, Auto-refresh, Copywriting contract)
    - microuis/mui-labeling/src/views/LabelingView.vue (top-level view structure, header breadcrumb, dark-mode localStorage key, onMounted)
    - microuis/mui-labeling/src/services (axios service-client convention through the BFF; confirm base URL/axios instance usage)
    - microuis/mui-observability/src/components/observability/StatusCard.vue and UptimeSummary.vue and RangeSelector.vue and LatencyTrendChart.vue (props/events from Plan 03)
    - microuis/mui-observability/src/routes.ts (the view path/name this file backs)
  </read_first>
  <action>
    Create src/services/observability.ts exporting fetchServices() → GET /observability/health/services and fetchMetrics(range) → GET /observability/metrics?range={range}, using the same axios instance/import convention as mui-labeling's service layer (shared axios singleton, BFF base path — calls go to /observability/* which the BFF proxies). Type the responses per the interfaces block, including `MetricsItem.history: Array<{ ts: string; avg_latency_ms: number | null }>`.

    Create ObservabilityView.vue composing the dashboard per the UI-SPEC Layout Contract:
    - Page header: h1 "Observability" (text-headline-lg font-bold) + subtitle "Real-time health, latency trends, and uptime SLOs across platform services." (text-body-md text-on-surface-variant). Reuse the LabelingView header/breadcrumb shell and the dark-mode localStorage convention renamed to 'mui-observability-theme'.
    - RangeSelector bound with v-model to a `range` ref (default '24h', top-right).
    - "Service Status" section: grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-md of 5 StatusCard, one per service (map service_name → display name), fed from fetchServices().
    - "Latency Trend" section: LatencyTrendChart fed from the metrics `history` series. Build the chart inputs from fetchMetrics() as follows (this is the resolved data contract — do NOT invent an alternative shaping):
        * labels: derive the ordered union of all `ts` bucket timestamps across the 5 services' `history` arrays (they share the same bucket boundaries for a given range; in practice each service has the same bucket set, so labels = the longest/first service's `history.map(h => h.ts)`). Format each `ts` for display per range if desired (e.g. hour-of-day for 24h, date for 7d/30d) — display formatting only, do not drop points.
        * datasets: one entry per service — { label: <display name>, borderColor: <UI-SPEC service color>, data: <that service's history avg_latency_ms aligned to the label order; use the per-bucket value, null where a bucket is missing so chart.js gaps the line> }.
      Bind these to <LatencyTrendChart :datasets="..." :labels="..." />. If `history` is empty for all services (no samples collected yet), render the section's Empty state copy rather than an empty chart.
    - "Uptime (SLO)" section: grid of 5 UptimeSummary fed from fetchMetrics() (uptime_pct → uptimePct, error_rate_pct → errorRatePct, p95/p99 mapped).
    - Behavior: on mount fetch both; setInterval every 15s to re-fetch ONLY fetchServices() (status cards), clear on onUnmounted; re-fetch fetchMetrics() ONLY when `range` changes (watch). Implement Loading ("Loading service health…"), Empty ("No health data yet" + body), and Error ("Unable to load observability data…") states per the Copywriting contract; the error state must be scoped to this view (never crash the Shell — PRD §10.2).
  </action>
  <verify>
    <automated>cd microuis/mui-observability && pnpm exec vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - services/observability.ts exports fetchServices() and fetchMetrics(range) calling /observability/health/services and /observability/metrics?range=, with the metrics item type including `history: { ts, avg_latency_ms }[]`
    - ObservabilityView renders the page header, a v-model RangeSelector defaulting to '24h', 5 StatusCards, LatencyTrendChart, and 5 UptimeSummary cards
    - The LatencyTrendChart receives `labels` derived from the history bucket timestamps and one `dataset` per service built from that service's history avg_latency_ms (no placeholder/synthetic series)
    - Status cards re-poll every 15s via setInterval cleared in onUnmounted; metrics re-fetch on range change only
    - Loading/Empty/Error states are present with the exact UI-SPEC copy; the error path does not throw past the view
    - `vue-tsc --noEmit` passes for the package
  </acceptance_criteria>
  <done>ObservabilityView composes all components with live data (chart fed from the real history series), auto-refresh, and the three non-happy states, and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Shell registration (vite.config, REMOTE_MANIFEST, importRemote, .env) + MainLayout nav + build smoke</name>
  <files>portal/vite.config.ts, portal/src/router/index.ts, portal/.env.example, portal/src/components/layout/MainLayout.vue</files>
  <read_first>
    - portal/vite.config.ts (lines ~28-30: mui-labeling remotes[] entry to mirror)
    - portal/src/router/index.ts (line 29 REMOTE_MANIFEST array; lines 59-78 importRemote switch — mirror the mui-labeling case)
    - portal/.env.example (full file — append the new var after VITE_REMOTE_LABELING)
    - portal/src/components/layout/MainLayout.vue (Audit Log nav button ~240-255; mui-labeling nav button ~185-197; breadcrumb label computed ~311; how authStore.hasRole and remoteStatuses are used)
    - .planning/phases/17-observabilidad-sla-slo/17-UI-SPEC.md (Navigation/Access Contract: exact nav button markup, icon monitor_heart, role gate)
  </read_first>
  <action>
    Edit portal/vite.config.ts: directly below the mui-labeling remotes entry add `remotes['mui-observability'] = env.VITE_REMOTE_OBSERVABILITY ? `${env.VITE_REMOTE_OBSERVABILITY}/assets/remoteEntry.js` : 'http://localhost:5180/assets/remoteEntry.js'`.

    Edit portal/src/router/index.ts: append to REMOTE_MANIFEST `{ name: 'mui-observability', envVar: 'VITE_REMOTE_OBSERVABILITY', displayName: 'Observability', pathPrefix: 'observability' }`; add an importRemote switch case `case 'mui-observability': return import('mui-observability/routes')` with the `// @ts-ignore - Module Federation remote` comment, mirroring the mui-labeling case.

    Edit portal/.env.example: append `VITE_REMOTE_OBSERVABILITY=http://localhost:5180` after the VITE_REMOTE_LABELING line.

    Edit portal/src/components/layout/MainLayout.vue: add the Observability nav button using the exact markup from UI-SPEC Navigation/Access Contract — `v-if="authStore.hasRole('PlatformAdmin') || authStore.hasRole('TenantAdmin') || authStore.hasRole('TenantOwner')"`, `@click="router.push('/observability')"`, disabled/opacity when `remoteStatuses['mui-observability'] === 'error'`, active highlight via `isActive('/observability')`, icon `<span class="material-symbols-outlined text-[22px]">monitor_heart</span>`, label "Observability". Place it adjacent to the Audit Log / Labeling nav buttons following the same structure used there (verify the exact hasRole/isActive/remoteStatuses helpers in this file before copying). Add the breadcrumb label: `if (segment === 'observability') return 'Observability'` in the breadcrumbLabel computed (~line 311).

    After edits, build the remote to prove federation exposure: `cd microuis/mui-observability && pnpm build`.
  </action>
  <verify>
    <automated>cd microuis/mui-observability && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - portal/vite.config.ts has a remotes['mui-observability'] entry defaulting to http://localhost:5180/assets/remoteEntry.js
    - portal/src/router/index.ts REMOTE_MANIFEST includes the mui-observability entry AND importRemote has a `case 'mui-observability': ... import('mui-observability/routes')`
    - portal/.env.example contains VITE_REMOTE_OBSERVABILITY=http://localhost:5180
    - MainLayout.vue renders an Observability nav button gated by hasRole('PlatformAdmin'|'TenantAdmin'|'TenantOwner') with the monitor_heart icon, and breadcrumbLabel returns 'Observability' for the 'observability' segment
    - `pnpm build` (mui-observability) succeeds and emits dist/assets/remoteEntry.js
  </acceptance_criteria>
  <done>The Shell registers and can load mui-observability; the role-gated Observability nav item and breadcrumb are wired; the remote builds with a remoteEntry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Observability dashboard: backend health checker + endpoints (Plans 01/02), tested components (Plan 03), and the composed ObservabilityView registered as a Shell remote with a role-gated nav item (this plan).</what-built>
  <how-to-verify>
    1. Start the stack: backend (`cd backend && uvicorn app.main:app --reload`), BFF (`cd bff && pnpm dev`), the mui-observability remote (`cd microuis/mui-observability && pnpm build:watch` or `pnpm preview` on 5180), and the Shell (`cd portal && pnpm dev` with VITE_REMOTE_OBSERVABILITY set in portal/.env).
    2. Wait ~30s after the backend starts so the health checker writes a few sample cycles.
    3. Log in as PlatformAdmin. Confirm the "Observability" nav item (monitor_heart icon) appears. Click it.
    4. Confirm: 5 Service Status cards (FastAPI Core, BFF, MySQL, Keycloak, WebSocket Gateway) with UP/DEGRADED/DOWN badges + current latency; a Latency Trend chart with up to 5 lines plotted from the per-bucket history series + the dashed 100ms SLO line; 5 Uptime (SLO) cards with uptime % (color-tiered) + p95/p99 + error rate.
    5. Switch the range selector between 24h / 7d / 30d — the trend chart re-fetches and re-buckets (hourly for 24h, daily for 7d/30d) and the uptime cards re-fetch; status cards keep refreshing ~every 15s.
    6. Log out, log in as TenantOwner (and/or TenantAdmin): confirm the nav item still appears and the same read-only dashboard loads.
    7. Log in as a role NOT in {PlatformAdmin,TenantOwner,TenantAdmin} (e.g. TenantViewer): confirm the Observability nav item is NOT shown.
    8. Optional resilience check: stop the BFF or Keycloak briefly and confirm the corresponding status card flips to DEGRADED/DOWN on the next cycle without crashing the rest of the portal.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/role/data issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → BFF → backend | Authenticated, role-gated dashboard data path |
| Shell host → remote | Shell dynamically loads mui-observability remoteEntry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Elevation of Privilege | Observability nav item / route | mitigate | Nav v-if gates on hasRole(PlatformAdmin/TenantAdmin/TenantOwner) AND routes.ts meta.roles enforces the same; backend/BFF independently enforce (defense in depth) |
| T-17-10 | Denial of Service | view error state on backend/BFF down | mitigate | Error state scoped to the view (PRD §10.2); RemoteErrorBoundary + remoteStatuses disable the nav when the remote fails — Shell stays functional |
| T-17-SC | Tampering | npm/pip installs | accept | No new packages introduced in this plan (chart deps installed in Plan 03) — no legitimacy gate needed |
</threat_model>

<verification>
- `cd microuis/mui-observability && pnpm build` emits dist/assets/remoteEntry.js (OBS-07)
- `cd microuis/mui-observability && pnpm exec vue-tsc --noEmit` passes
- `pnpm -w test` full frontend suite green (wave merge gate)
- Human checkpoint: dashboard renders for PlatformAdmin/TenantOwner/TenantAdmin; hidden for other roles
</verification>

<success_criteria>
- ObservabilityView composes all four components with live BFF data + 15s status polling + range-driven metrics refresh
- LatencyTrendChart is fed from the real per-service history series (datasets + labels), not a placeholder
- mui-observability registered as a Shell remote on port 5180 and builds a remoteEntry (OBS-07)
- Role-gated Observability nav item + breadcrumb wired in MainLayout (OBS-08, D-10/D-11)
- Dashboard verified in-browser across roles via the human checkpoint
</success_criteria>

<output>
Create `.planning/phases/17-observabilidad-sla-slo/17-04-SUMMARY.md` when done
</output>
</content>
