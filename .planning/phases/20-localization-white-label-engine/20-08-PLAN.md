---
phase: 20-localization-white-label-engine
plan: 08
type: execute
wave: 6
depends_on: ["20-07", "20-09"]
files_modified:
  - microuis/mui-labeling/src/components/labeling/TranslationDrawer.vue
  - microuis/mui-labeling/src/components/labeling/AddKeyModal.vue
  - microuis/mui-labeling/src/components/labeling/ImportExportModal.vue
  - microuis/mui-labeling/src/components/labeling/DiagnosticsModal.vue
  - microuis/mui-labeling/src/views/LabelingView.vue
  - microuis/mui-labeling/src/components/labeling/KeysMatrix.vue
  - microuis/mui-labeling/src/composables/useLabelingState.ts
  - microuis/mui-labeling/src/services/labels.ts
autonomous: true
requirements: [LBL-14, LBL-15]

must_haves:
  truths:
    - "Selecting a key in KeysMatrix opens TranslationDrawer showing es_PE/en_US editors, parameter hints, and an inheritance tree with 'Hereda de Tenant/Company' badges"
    - "Saving the drawer form calls PATCH /labels/keys/{id} (structure) or /value (UXWriter), surfaces a 409 toast with the exact PI-02 message on version conflict, and refreshes the matrix"
    - "RF-05 Restore action calls POST /labels/keys/restore and is disabled when no override exists at the active level"
    - "Add Key modal creates a new label with both locale values and param validation against {var} placeholders found in the values"
    - "Export modal produces a JSON file (SDK bootstrap shape) and a CSV file (translator-friendly) for the active workspace context + namespace, with no import functionality (export-only per CONTEXT.md)"
    - "Diagnostics modal lists missing_label_reports for the active tenant with hits/last_reported_at, and 'quick create' pre-fills the Add Key modal"
    - "Dark mode toggle persists to localStorage and applies dark: Tailwind classes across all labeling components"
  artifacts:
    - path: "microuis/mui-labeling/src/components/labeling/TranslationDrawer.vue"
      provides: "RF-04 key editor + RF-05 inheritance tree/restore"
      contains: "Hereda de"
    - path: "microuis/mui-labeling/src/components/labeling/ImportExportModal.vue"
      provides: "RF-07 export-only (JSON + CSV)"
      contains: "text/csv"
    - path: "microuis/mui-labeling/src/components/labeling/DiagnosticsModal.vue"
      provides: "RF-06 missing-keys diagnostics + quickCreateMissing"
      contains: "listMissingLabels"
  key_links:
    - from: "microuis/mui-labeling/src/components/labeling/TranslationDrawer.vue"
      to: "microuis/mui-labeling/src/services/labels.ts"
      via: "updateKey/updateKeyValue/restoreOverride calls"
      pattern: "restoreOverride"
    - from: "microuis/mui-labeling/src/components/labeling/DiagnosticsModal.vue"
      to: "microuis/mui-labeling/src/components/labeling/AddKeyModal.vue"
      via: "quickCreateMissing pre-fills shared state consumed by AddKeyModal"
      pattern: "quickCreate"
    - from: "microuis/mui-labeling/src/components/labeling/ImportExportModal.vue"
      to: "backend/app/domains/labels/router.py"
      via: "exportNamespace() calls GET /labels/export?format=json|csv (from 20-09)"
      pattern: "exportNamespace"
---

<objective>
Complete the `mui-labeling` admin UI by implementing the right-hand `TranslationDrawer.vue` (RF-04 key editor with parameter validation + RF-05 inheritance tree/restore), `AddKeyModal.vue` (key creation with param validation), `ImportExportModal.vue` (RF-07 export-only JSON/CSV), `DiagnosticsModal.vue` (RF-06 missing-keys panel + quick-create), and RF-08 dark mode wiring across the module. Wire all of these into `LabelingView.vue` (replacing the 20-07 placeholder drawer) and `KeysMatrix.vue` (Add Key button, diagnostics/import-export entry points).

Purpose: This is the final plan for the admin micro-UI, completing all v1 RFs (01-08) per `docs/prd_namespaces_keys_management.md` and the HTML prototype.
Output: Fully functional mui-labeling admin module — namespace/key CRUD, inheritance visualization with restore, export, missing-key diagnostics, dark mode.
</objective>

<execution_context>
@C:/Users/ianache/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-localization-white-label-engine/20-CONTEXT.md
@.planning/phases/20-localization-white-label-engine/20-RESEARCH.md
@docs/prd_namespaces_keys_management.md
@design/stitch/labeling - namespaces_keys_management.html
@microuis/mui-labeling/src/views/LabelingView.vue
@microuis/mui-labeling/src/components/labeling/NamespaceSidebar.vue
@microuis/mui-labeling/src/components/labeling/KeysMatrix.vue
@microuis/mui-labeling/src/services/labels.ts
@microuis/mui-labeling/src/composables/useLabelingState.ts
</context>

<interfaces>
<!-- services/labels.ts from 20-07 -->
```typescript
export async function updateKey(id: number, payload: LabelUpdatePayload): Promise<LocalizedLabel>  // version conflict -> 409 with PI-02 message in response body's `detail`
export async function updateKeyValue(id: number, payload: LabelValuePayload): Promise<LocalizedLabel>  // same 409 behavior
export async function restoreOverride(payload: RestorePayload): Promise<void>  // 404 if no override at this level
export async function listMissingLabels(tenantId: string): Promise<MissingLabelReport[]>
export async function createKey(payload: LabelCreatePayload): Promise<LocalizedLabel[]>
export async function listKeys(filters: KeysFilter): Promise<LocalizedLabel[]>
```

<!-- useLabelingState composable from 20-07 (shared reactive state) -->
```typescript
// provides: activeNamespace, workspaceContext {tenantId, companyId, productId}, searchQuery, selectedKey, namespaces[], keys[]
// + refresh functions: refreshNamespaces(), refreshKeys()
```

<!-- PI-02 concurrency message (exact, from backend 409 detail) -->
"La clave ha sido modificada por otro usuario. Por favor, recargue el editor para no perder los cambios."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TranslationDrawer.vue (RF-04 editor + RF-05 inheritance tree/restore)</name>
  <files>microuis/mui-labeling/src/components/labeling/TranslationDrawer.vue, microuis/mui-labeling/src/views/LabelingView.vue, microuis/mui-labeling/src/composables/useLabelingState.ts</files>
  <action>
Reference `design/stitch/labeling - namespaces_keys_management.html` lines ~426-526 (`#drawerBody`, `#drawerEmptyState`, `#drawerEditForm`, `#drawerKeyTitle`, `#drawerKeyDesc`, `#drawerKeyParamsContainer`/`#drawerKeyParams`, `#inputEsPE`/`#inputEnUS` + `#esBadge`/`#enBadge` ("Heredado" badges), the inheritance preview block (`#resolValTenant`/`#resolValCompany`/`#resolCompanyBadge`/`#resolValProduct`/`#resolProductBadge`, lines ~490-512, with "Hereda de Tenant"/"Hereda de Company / Tenant" text), `#saveKeyBtn`, `#restoreBtn` (lines ~517-524).

Create `microuis/mui-labeling/src/components/labeling/TranslationDrawer.vue`:

- **Empty state**: when `selectedKey` (from `useLabelingState`) is `null`, show the prototype's `#drawerEmptyState` placeholder ("Selecciona una clave para ver detalles" or similar copy from the prototype).
- **Edit form** (when `selectedKey` is set):
  - Header: `label_key` (monospace/code style), `label_type` badge, `description`.
  - If `params` is non-empty, show the params hint block (`#drawerKeyParamsContainer`/`#drawerKeyParams`) listing `{var}` placeholders.
  - Two textareas for `es_PE` and `en_US` values, each bound to local editable refs initialized from the rows in `keys[]` matching `selectedKey.label_key` at the ACTIVE workspace level (most specific of product > company > tenant — i.e., the row actually returned for the current context). Show an "Heredado" badge next to each locale if the value being displayed comes from a higher level than the active context (compare: does a row exist at the exact `{tenant_id, company_id, product_id}` of the active context for this key+locale? If not, it's inherited — fetch/derive this by also querying `listKeys()` with the exact active-context scope, or by including scope info in each row already; if `listKeys()` as implemented in 20-07 returns rows from ALL applicable levels per `service.list_labels`'s additive filter, use each row's own `company_id`/`product_id` to determine its level and compare against the active `workspaceContext`).
  - **Inheritance tree (RF-05)**: below the editors, render 3 rows — Tenant / Company / Product — each showing the resolved value at that level (or "Hereda de Tenant" / "Hereda de Company / Tenant" placeholder text per the prototype if no override exists at that level) and a small badge ("No select" if no company/product is selected in the workspace context, matching `#resolCompanyBadge`/`#resolProductBadge`).
  - **Save button** (`#saveKeyBtn`): 
    - If the active context level (company_id+product_id both set -> product level; company_id set, product_id null -> company level; both null -> tenant level) has an existing row for this key+locale, call `updateKeyValue(id, {locale, label_value, version})` for UXWriter-role users, OR `updateKey(id, {values: {...}, params, description, label_type, version})` for structure-role users — check `useUserContext().roles` to decide which endpoint (mirror the backend's `_require_value_role`/`_require_structure_role` split: if roles only intersect `UXWriter` and not the structure roles, use `updateKeyValue`).
    - If NO row exists at the active context level for this key (i.e., it's purely inherited), saving creates a new override row via `createKey()` with the active context's `tenant_id`/`company_id`/`product_id`.
    - On success: show a success toast (`shell/toastStore`), call `refreshKeys()`, update `selectedKey` to the refreshed row.
    - On 409: show a toast with EXACTLY the message `"La clave ha sido modificada por otro usuario. Por favor, recargue el editor para no perder los cambios."` (read from the error response body's `detail` field — do not hardcode unless the backend message differs, in which case display whatever `detail` the 409 response contains, which per 20-03 IS this exact string), and offer a "Reload" action that re-fetches the row's current `version`/values from `listKeys()`.
  - **Restore button** (`#restoreBtn`, RF-05 "Restaurar"):
    - Disabled (matching prototype's `opacity-50`/`disabled` toggling, lines ~1055-1062) when no override row exists at the active context level for this key+locale.
    - When enabled and clicked, calls `restoreOverride({tenant_id, company_id, product_id, namespace, locale, label_key})` for BOTH locales (or per-locale if the UI distinguishes — match the prototype's single button restoring the whole key's override at this level for both locales). On success, refresh keys/drawer; on 404 ("No override found at this level"), show an info toast and disable the button.

Add a `quickCreatePrefill` field to `useLabelingState.ts`'s shared state (a `ref<{namespace: string; label_key: string} | null>(null)`) — DiagnosticsModal (Task 3) will set this to open AddKeyModal pre-filled; TranslationDrawer doesn't need to read it but it must exist in the shared composable for Task 3.

In `LabelingView.vue`, replace the 20-07 placeholder `<aside>` with `<TranslationDrawer class="col-span-3" />`.
  </action>
  <verify>
    <automated>cd microuis/mui-labeling && npx vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>TranslationDrawer shows empty/edit states correctly, RF-04 param hints render, RF-05 inheritance tree shows "Hereda de Tenant/Company" with correct badges, save handles both UXWriter value-only and structure roles plus 409 PI-02 toast, restore is correctly enabled/disabled and calls the restore endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: AddKeyModal.vue (RF-04 creation + param validation) + KeysMatrix wiring</name>
  <files>microuis/mui-labeling/src/components/labeling/AddKeyModal.vue, microuis/mui-labeling/src/components/labeling/KeysMatrix.vue</files>
  <action>
Reference prototype lines ~569-617 (`#addKeyModal`, `#keyNameInput`, `#keyTypeInput`, `#keyParamsInput`, `#keyDescInput`, `#keyEsInput`/`#keyEnInput`).

Create `microuis/mui-labeling/src/components/labeling/AddKeyModal.vue`:
- Fields: `label_key` (text, validated against a key-naming convention — check prototype/PRD for the regex, likely `^[a-z][a-z0-9_]*$` matching `btn_aceptar`/`lbl_document_id` style; if unspecified, use `^[a-z][a-z0-9_]*$`), `label_type` (select: LABEL/PLACEHOLDER/VALIDATION/TOOLTIP per `VALID_LABEL_TYPES` from 20-01 schemas), `params` (text input, comma-separated `{var}` names e.g. `min, username` — parse into `string[]`), `description` (text), `es_PE` value (textarea), `en_US` value (textarea).
- **Param validation**: after parsing `params` into a list, validate that every `{param}` placeholder referenced in either locale's value (via regex `/\{(\w+)\}/g`) is present in the `params` list, and warn (non-blocking inline message, e.g. "Unrecognized placeholder {xyz} — add it to Params") if not. This is the "parameter validation" referenced in RF-04/LBL-08's interpolation contract.
- On submit, calls `createKey({ tenant_id, company_id, product_id (from active workspaceContext), namespace: activeNamespace, label_key, label_type, params, description, values: { es_PE, en_US } })`. On success: toast, `refreshKeys()`, close modal. On 409 (duplicate key at this scope — if the backend enforces a unique constraint on `(tenant_id, company_id, product_id, namespace, locale, label_key)` per 20-01's `ux_localized_labels_scope_key` index), show an inline error near `label_key`.
- Accept an optional `prefill: { namespace?: string; label_key?: string }` prop (consumed via `useLabelingState`'s `quickCreatePrefill` from Task 1/3) — when set, pre-populate `label_key` (and switch `activeNamespace` to `prefill.namespace` if different) when the modal opens.

In `KeysMatrix.vue`, wire the "+ Add Key" button (left as a TODO in 20-07) to open `AddKeyModal` (render it conditionally with a `showAddKeyModal` ref local to KeysMatrix or lifted to `useLabelingState` if DiagnosticsModal needs to trigger it externally — prefer lifting `showAddKeyModal` to `useLabelingState` so DiagnosticsModal's quick-create can open it too).
  </action>
  <verify>
    <automated>cd microuis/mui-labeling && npx vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>AddKeyModal creates new label keys with both locale values, validates {param} placeholders against the params list, handles duplicate-key 409s, and supports pre-fill from DiagnosticsModal's quick-create.</done>
</task>

<task type="auto">
  <name>Task 3: ImportExportModal.vue (RF-07 export-only) + DiagnosticsModal.vue (RF-06) + dark mode (RF-08)</name>
  <files>microuis/mui-labeling/src/components/labeling/ImportExportModal.vue, microuis/mui-labeling/src/components/labeling/DiagnosticsModal.vue, microuis/mui-labeling/src/views/LabelingView.vue, microuis/mui-labeling/src/composables/useLabelingState.ts</files>
  <action>
**`ImportExportModal.vue`** (RF-07, export-only per CONTEXT.md — reference prototype lines ~619-669, but OMIT the import tab/`#ieImportContent`/`#btnTabImport`/`triggerImport` entirely; show ONLY the export UI `#ieExportContent`):
- Export scope: active namespace + active workspace context (tenant/company/product as currently selected).
- Add `exportNamespace(format: 'json' | 'csv', filters: KeysFilter & { namespace: string })` to `microuis/mui-labeling/src/services/labels.ts` (added to this plan's `files_modified`), calling the `GET /labels/export?...&format=...` endpoint from 20-09 with `responseType: 'blob'` (axios option) so binary/text content is returned as a `Blob` regardless of `Content-Type`:
  ```typescript
  export async function exportNamespace(
    format: 'json' | 'csv',
    filters: KeysFilter & { namespace: string },
  ): Promise<Blob> {
    const { data } = await api.get('/labels/export', {
      params: { ...filters, format },
      responseType: 'blob',
    })
    return data
  }
  ```
- Two export buttons, both calling `exportNamespace()` then triggering a browser download via `URL.createObjectURL(blob)` + a temporary `<a download>` click:
  - **"Export JSON"**: `exportNamespace('json', {...activeContext, namespace: activeNamespace})`, filename `${namespace}_${tenantId}.json` — body is the `export_namespace_json()` shape from 20-09 (`{ [namespace]: { [label_key]: { es_PE, en_US } } }`).
  - **"Export CSV"**: `exportNamespace('csv', {...activeContext, namespace: activeNamespace})`, filename `${namespace}_${tenantId}.csv` (the backend already sets `Content-Disposition` with this filename — read it from the response headers if `api` exposes them, otherwise reuse the same naming convention client-side) — body is the `export_namespace_csv()` CSV from 20-09 (columns `namespace,label_key,es_PE,en_US,level`).
- No drag-and-drop, no file input, no "Analizar Archivo" button — RF-07 v1 is export-only (CONTEXT.md locked decision). No client-side CSV/JSON serialization — both formats are produced by the backend `/labels/export` endpoint (20-09), matching RESEARCH.md's "Don't Hand-Roll" guidance (Python stdlib `csv` module).

**`DiagnosticsModal.vue`** (RF-06, reference prototype lines ~671-735):
- On open, calls `listMissingLabels(workspaceContext.tenantId)` and renders a table: `namespace`, `label_key`, `locale`, `hits`, `last_reported_at` (formatted as relative or absolute date).
- Each row has a "Quick Create" action: sets `useLabelingState`'s `quickCreatePrefill = { namespace: row.namespace, label_key: row.label_key }`, sets `showAddKeyModal = true`, and closes the diagnostics modal — AddKeyModal (Task 2) picks up the prefill via its `prefill` prop/shared state.
- Empty state: "No missing labels reported" / equivalent Spanish copy from the prototype if available.

**Dark mode (RF-08)**, reference prototype's `themeToggleBtn` logic near the end of the `<script>` block (~line 1347+):
- Add a `useDarkMode` composable (or inline logic in `LabelingView.vue`) that:
  - On mount, reads `localStorage.getItem('mui-labeling-theme')` (or a shared key if the portal already has a dark-mode convention — check `portal/src/` for an existing dark-mode localStorage key/composable and REUSE it if one exists, for consistency with the rest of the app, instead of inventing a module-local one).
  - Toggles a `dark` class on `document.documentElement` (Tailwind's `darkMode: 'class'` strategy — verify `tailwind.config.js` from 20-07 has `darkMode: 'class'`; if it's `'media'`, change to `'class'` to support a manual toggle, matching the prototype's explicit toggle button).
  - Persists the choice to `localStorage` on toggle.
- Wire the toggle button in `LabelingView.vue`'s header (the `#themeToggleBtn` equivalent from the prototype).
- Audit all components created in 20-07/20-08 for `dark:` variant classes matching the prototype's dark palette (`dark:bg-slate-950`, `dark:bg-slate-900`, `dark:text-slate-200`, `dark:border-slate-800`, etc.) — this task should do a final pass adding any missing `dark:` classes to ensure the whole module looks correct in dark mode, since 20-07 components were built primarily against light-mode prototype classes.

Wire both modals into `LabelingView.vue`: render `<ImportExportModal v-if="showImportExport" />` and `<DiagnosticsModal v-if="showDiagnostics" />`, with header buttons (or KeysMatrix toolbar buttons) toggling `showImportExport`/`showDiagnostics` refs in `useLabelingState`.
  </action>
  <verify>
    <automated>cd microuis/mui-labeling && npx vue-tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <done>Export produces valid JSON and CSV downloads scoped to the active namespace+context, diagnostics lists missing_label_reports with working quick-create into AddKeyModal, dark mode toggle persists and applies across all labeling components, and the whole mui-labeling package builds successfully.</done>
</task>

</tasks>

<verification>
- `cd microuis/mui-labeling && npx vue-tsc --noEmit` — no type errors
- `cd microuis/mui-labeling && npm run build` — succeeds
- `cd microuis/mui-labeling && npm run preview` then manually exercise: select a key -> drawer shows inheritance tree -> edit + save -> 409 on stale version shows PI-02 toast -> restore disabled/enabled correctly -> Add Key with param validation -> Export JSON/CSV downloads -> Diagnostics lists missing keys -> quick-create opens AddKeyModal pre-filled -> dark mode toggle persists across reload
</verification>

<success_criteria>
- RF-04: key editor with param hints, save via correct endpoint per role (UXWriter value-only vs structure roles), 409 PI-02 handling
- RF-05: inheritance tree with "Hereda de Tenant/Company" + working restore (disabled when no override exists)
- RF-06: diagnostics modal + quickCreateMissing flow into AddKeyModal
- RF-07: export-only JSON (SDK-bootstrap-like shape) + CSV, no import UI
- RF-08: dark mode toggle persisted, applied across all mui-labeling components
- Full mui-labeling package builds successfully — Phase 20 admin UI complete
</success_criteria>

<output>
After completion, create `.planning/phases/20-localization-white-label-engine/20-08-SUMMARY.md`
</output>
