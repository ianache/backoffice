# Deferred Items — Phase 20 (Localization White Label Engine)

## From Plan 20-01

- **LBL-01, LBL-02, LBL-16 not present in `.planning/REQUIREMENTS.md`** — `requirements mark-complete` returned `not_found` for all three IDs. These requirement IDs are referenced in `20-01-PLAN.md` frontmatter (and presumably 20-CONTEXT.md/20-RESEARCH.md) but were never added to the master REQUIREMENTS.md traceability table. Out of scope for this plan to retroactively create requirements entries — flagging for the phase owner / next planning pass to add LBL-* requirements to REQUIREMENTS.md so future `mark-complete` calls succeed.
- **Backend Python dependencies not installed in any accessible environment** — `sqlalchemy`, `alembic`, `pydantic`, `fastapi` are absent from system Python and no `backend/.venv` exists. Plan 20-01's specified verification commands (`python -c "from app.domains.labels.models import ..."`, `python -m alembic heads`) could not run as written; substituted with `python -m py_compile` syntax checks and a structural check for migration chain conflicts (no other migration declares `down_revision='e001'`). Recommend running `pip install -r backend/requirements.txt` (in a dedicated venv) and `alembic upgrade head` against the dev DB before/during Plan 02 to confirm the g001/g002 migration chain applies cleanly.

## From Plan 20-02

- **LBL-03, LBL-04, LBL-13 not present in `.planning/REQUIREMENTS.md`** — same gap as 20-01's LBL-01/LBL-02/LBL-16. `requirements mark-complete LBL-03 LBL-04 LBL-13` returned `not_found` for all three IDs. Add all LBL-* IDs referenced across Phase 20 plans to REQUIREMENTS.md's traceability table.
- **`pytest` not runnable — sqlalchemy still not installed** — same limitation as 20-01. `backend/app/domains/labels/service.py`, `backend/tests/test_labels_resolve.py`, and `backend/tests/test_labels_service.py` were verified via `python -m py_compile` (syntax-only). The plan's specified verification commands (`python -m pytest tests/test_labels_resolve.py -x -q`, `python -m pytest tests/test_labels_service.py -x -q`) could not be executed. Once `backend/requirements.txt` is installed, run `python -m pytest backend/tests/test_labels_resolve.py backend/tests/test_labels_service.py -q` and the full suite to confirm all 11 new tests pass with no regressions.

## From Plan 20-09

- **LBL-14 not present in `.planning/REQUIREMENTS.md`** — same traceability gap as 20-01/20-02's LBL-* IDs. `requirements mark-complete LBL-14` will likely return `not_found`. Add LBL-14 (and all other LBL-* IDs referenced across Phase 20 plans) to REQUIREMENTS.md's traceability table.
- **`pytest` not runnable — sqlalchemy still not installed** — same limitation as 20-01/20-02. `backend/app/domains/labels/service.py` (export_namespace_json/csv additions), `backend/app/domains/labels/router.py` (/export endpoint), and `backend/tests/test_labels_export.py` (7 tests: Tests 1-4 service-level, Tests 5-7 router-level) were verified via `python -m py_compile` (syntax-only). Once `backend/requirements.txt` is installed, run `python -m pytest backend/tests/test_labels_export.py -q` to confirm all 7 tests pass.
- **`backend/app/domains/labels/router.py` shared-file race with concurrent plans 20-03/20-04** — at the time this plan's Task 2 commit (d76293c) was made, `router.py` was untracked in the shared worktree (20-03's base namespace/label CRUD endpoints had not yet been separately committed by 20-03). This plan's commit captured the full router.py content as it existed at that point (20-03's CRUD endpoints + this plan's additive `/export` endpoint) to avoid losing the `/export` endpoint to a subsequent overwrite. 20-04's INVALIDATE_NAMESPACE WS broadcast wiring (verified via test_labels_sdk_router.py's Test 6, which expects `PATCH /labels/keys/{label_id}/value` to call `app.state.ws_manager.broadcast(...)`) was NOT present in router.py at commit time and was not added by this plan (out of scope). If 20-04's WS wiring commit conflicts with or is superseded by d76293c, the phase owner should verify `update_label_value`'s router handler still broadcasts `INVALIDATE_NAMESPACE` after both commits are merged — re-add the wiring if it was dropped.

## From Plan 20-03

- **LBL-09, LBL-10, LBL-11, LBL-12 not present in `.planning/REQUIREMENTS.md`** — same traceability gap as other Phase 20 plans. `requirements mark-complete LBL-09 LBL-10 LBL-11 LBL-12` will likely return `not_found`. Add these (and all other LBL-* IDs referenced across Phase 20 plans) to REQUIREMENTS.md's traceability table.
- **`pytest` not runnable — sqlalchemy still not installed** — same limitation as 20-01/20-02/20-04/20-09. `backend/app/domains/labels/router.py` (namespace + label CRUD, role gating, audit logging, 409 optimistic concurrency, restore-override endpoint, `/missing` diagnostics), `backend/app/main.py` (router registration), `backend/tests/test_labels_router.py` (7 tests), and `backend/tests/test_labels_missing.py` (2 tests) were verified via `python -m py_compile` (syntax-only). Once `backend/requirements.txt` is installed, run `python -m pytest backend/tests/test_labels_router.py backend/tests/test_labels_missing.py -q` to confirm all 9 tests pass.
- **Router registration: `app.include_router(labels_router, prefix="/api/v1")`** — `router.py` declares `APIRouter(prefix="/labels", ...)`; `main.py` adds an additional `/api/v1` prefix at registration, so the labels admin API is mounted at `/api/v1/labels/*` (namespaces, keys, missing, export). This was chosen to match the pre-existing `test_labels_sdk_router.py` (20-04) expectations for `PATCH /api/v1/labels/keys/{label_id}/value`, even though `feature_flags`'s `flags_router` uses a bare `/flags` prefix (BFF prepends `/api/v1` when proxying). Flagging for the phase owner to confirm the BFF's `/api/v1/labels` proxy route (Plan 20-05+) does not double-prefix to `/api/v1/api/v1/labels`.
- **Missing import (Rule 1 fix) in concurrently-evolved `router.py`** — while this plan's `router.py` content was being concurrently extended by Plan 20-09's `/export` endpoint (using `JSONResponse`/`PlainTextResponse`), the import line `from fastapi.responses import JSONResponse, PlainTextResponse` was at one point absent, which would cause a `NameError` at runtime (not caught by `py_compile`, which is syntax-only). This was re-added to the top-level import block. Final `router.py` compiles cleanly and all 12 `@router.*` endpoint decorators (GET/POST /namespaces, PATCH/DELETE /namespaces/{id}, GET/POST /keys, PATCH /keys/{id}, PATCH /keys/{id}/value, DELETE /keys/{id}, POST /keys/restore, GET /missing, GET /export) are present and resolvable.
- **Commit attribution / shared-index race** — this plan's intended single commit for `router.py` + `main.py` + `test_labels_router.py` ("feat(20-03): add labels admin CRUD router with role checks and audit logging") was run via `git add <files> && git commit -m ...`, but a concurrent agent (20-04) had also staged files in the shared index at that moment. The resulting commit `6e111b6` (authored as "feat(20-04): broadcast INVALIDATE_NAMESPACE on label/namespace mutations") absorbed this plan's `router.py`/`main.py`/`test_labels_router.py` changes alongside 20-04's own WS-broadcast wiring and Test 6. A separate commit `2d9c6e1` (titled "feat(20-03): add labels admin CRUD router with role checks and audit logging") instead captured unrelated concurrent-agent files (`.planning/STATE.md`, `.planning/ROADMAP.md`, `20-09-SUMMARY.md`, `deferred-items.md`). No content was lost or reverted — verified via `git show --stat 6e111b6`, `git show HEAD:backend/app/main.py`, and `git ls-files backend/tests/test_labels_router.py` that all of this plan's deliverables are present and correct in HEAD. This is a commit-message/attribution mismatch only, not a content defect — consistent with the pattern already noted under "From Plan 20-04" and "From Plan 20-09" above for the same shared-file race.
- **`backend/tests/test_labels_missing.py`** was committed cleanly under this plan as `25138e7` (test(20-03): add missing-label-reports admin listing test) — 2 tests covering `GET /api/v1/labels/missing?tenant_id=...` (tenant-scoped, hits-descending) and a smoke test confirming the labels router is registered in `app.main`.

## From Plan 20-04

- **`pytest` not runnable — sqlalchemy still not installed** — same limitation as 20-01/20-02/20-09. `backend/app/domains/sdk/router.py` (labels bootstrap/prefetch/missing endpoints), `backend/app/domains/labels/router.py` (INVALIDATE_NAMESPACE broadcast wiring on create_key/update_key/update_key_value/delete_key/restore_override/delete_namespace), and `backend/tests/test_labels_sdk_router.py` (6 tests) were verified via `python -m py_compile` (syntax-only). Once `backend/requirements.txt` is installed, run `python -m pytest backend/tests/test_labels_sdk_router.py backend/tests/test_labels_router.py -q` to confirm all tests pass (Test 6's path resolution is dynamic via `app.routes` lookup to tolerate the `/labels` vs `/api/v1/labels` prefix decided by 20-03's `app/main.py` wiring).
- **LBL-05, LBL-06, LBL-07 not present in `.planning/REQUIREMENTS.md`** — same traceability gap as other Phase 20 plans. `requirements mark-complete LBL-05 LBL-06 LBL-07` will likely return `not_found`. Add these (and all other LBL-* IDs referenced across Phase 20 plans) to REQUIREMENTS.md's traceability table.
- **Commit attribution race on shared files (`backend/app/main.py`, `backend/tests/test_labels_router.py`)** — commit 6e111b6 (this plan's Task 2 commit) unintentionally swept in two files belonging to concurrent agents' work-in-progress: `backend/app/main.py` (20-03's `app.include_router(labels_router, prefix="/api/v1")` wiring, registering the labels router at `/api/v1/labels/*`) and `backend/tests/test_labels_router.py` (a new 308-line test file, likely from 20-03 or 20-09). This happened because `git add <files>` was run immediately after `git status --short`, and the concurrent agents wrote these files to disk in that window. Content of both files was verified via `python -m py_compile` and is syntactically correct/complete — no destructive action was taken (no revert/reset). The phase owner should confirm these files' content matches what 20-03/20-09 intended in their own SUMMARY.md files; if either agent's own commit is now a no-op (content already committed by 6e111b6), that's expected and harmless — just a commit-message attribution mismatch, not a content defect.
- **Backend route registration: `/api/v1/labels/*` vs `/flags`-style `/labels`** — `backend/app/main.py` now registers `labels_router` with `prefix="/api/v1"` (i.e., `app.include_router(labels_router, prefix="/api/v1")`), making the backend itself serve `/api/v1/labels/*`. This differs from the `feature_flags` router convention, where `flags_router` has `prefix="/flags"` and is included with no prefix override (backend serves `/flags/*`; the Node BFF prepends `/api/v1` when proxying). This inconsistency was introduced by a concurrent plan (20-03), not 20-04 — flagging for the phase owner to confirm whether the BFF's `/api/v1/labels` proxy route (if any, from 20-05+) double-prefixes to `/api/v1/api/v1/labels` or whether this is the intended new convention for Phase 20 domains.

## From Plan 20-05

- **LBL-10 not present in `.planning/REQUIREMENTS.md`** — `requirements mark-complete LBL-10` returned `not_found`, same traceability gap as other Phase 20 LBL-* IDs (see entries under 20-01/20-02/20-03/20-04/20-09 above). Add LBL-10 (and all other LBL-* IDs referenced across Phase 20 plans) to REQUIREMENTS.md's traceability table.
- **BFF `/api/v1/labels` proxy does NOT double-prefix** — resolved the open question raised in the "From Plan 20-04" entry above ("Backend route registration: `/api/v1/labels/*` vs `/flags`-style `/labels`"). `bff/src/routes/labels.ts` is mounted at Express path `/labels` (`app.use('/labels', labelsRouter)`), and its `pathRewrite` rebuilds the path as `/api/v1/labels${path}` (where `path` is the request path with the `/labels` mount prefix already stripped by Express). A request to `GET /labels/namespaces` therefore proxies to backend `GET /api/v1/labels/namespaces` — a single `/api/v1` prefix, matching the live route table confirmed via `app.routes` inspection (`/api/v1/labels/namespaces`, `/api/v1/labels/keys`, `/api/v1/labels/missing`, `/api/v1/labels/export`, etc.). No double-prefixing occurs. This is the intended new convention for the labels domain (distinct from flags.ts's `/flags${path}` rewrite, which targets the backend's bare `/flags/*` mount).

## From Plan 20-07

- **LBL-15 not present in `.planning/REQUIREMENTS.md`** — `requirements mark-complete LBL-15` returned `not_found`, same traceability gap as all other Phase 20 LBL-* IDs documented above (20-01/20-02/20-03/20-04/20-05/20-09). Confirmed via grep that `.planning/REQUIREMENTS.md` contains zero `LBL-*` entries — the master traceability table was never updated to include any Phase 20 requirement IDs. This plan's deliverables (mui-labeling scaffold, LabelingView grid shell, WorkspaceContextSelector/NamespaceSidebar/KeysMatrix for RF-01/02/03, portal WhiteLabels nav activation) are complete and verified independently of this tracking gap. Recommend the phase owner do a single consolidated pass adding all Phase 20 LBL-01 through LBL-16 IDs to REQUIREMENTS.md's traceability table once the phase nears completion, then re-run `requirements mark-complete` for the full set.
