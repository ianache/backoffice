# Deferred Items — Phase 20 (Localization White Label Engine)

## From Plan 20-01

- **LBL-01, LBL-02, LBL-16 not present in `.planning/REQUIREMENTS.md`** — `requirements mark-complete` returned `not_found` for all three IDs. These requirement IDs are referenced in `20-01-PLAN.md` frontmatter (and presumably 20-CONTEXT.md/20-RESEARCH.md) but were never added to the master REQUIREMENTS.md traceability table. Out of scope for this plan to retroactively create requirements entries — flagging for the phase owner / next planning pass to add LBL-* requirements to REQUIREMENTS.md so future `mark-complete` calls succeed.
- **Backend Python dependencies not installed in any accessible environment** — `sqlalchemy`, `alembic`, `pydantic`, `fastapi` are absent from system Python and no `backend/.venv` exists. Plan 20-01's specified verification commands (`python -c "from app.domains.labels.models import ..."`, `python -m alembic heads`) could not run as written; substituted with `python -m py_compile` syntax checks and a structural check for migration chain conflicts (no other migration declares `down_revision='e001'`). Recommend running `pip install -r backend/requirements.txt` (in a dedicated venv) and `alembic upgrade head` against the dev DB before/during Plan 02 to confirm the g001/g002 migration chain applies cleanly.

## From Plan 20-02

- **LBL-03, LBL-04, LBL-13 not present in `.planning/REQUIREMENTS.md`** — same gap as 20-01's LBL-01/LBL-02/LBL-16. `requirements mark-complete LBL-03 LBL-04 LBL-13` returned `not_found` for all three IDs. Add all LBL-* IDs referenced across Phase 20 plans to REQUIREMENTS.md's traceability table.
- **`pytest` not runnable — sqlalchemy still not installed** — same limitation as 20-01. `backend/app/domains/labels/service.py`, `backend/tests/test_labels_resolve.py`, and `backend/tests/test_labels_service.py` were verified via `python -m py_compile` (syntax-only). The plan's specified verification commands (`python -m pytest tests/test_labels_resolve.py -x -q`, `python -m pytest tests/test_labels_service.py -x -q`) could not be executed. Once `backend/requirements.txt` is installed, run `python -m pytest backend/tests/test_labels_resolve.py backend/tests/test_labels_service.py -q` and the full suite to confirm all 11 new tests pass with no regressions.

## From Plan 20-09

- **LBL-14 not present in `.planning/REQUIREMENTS.md`** — same traceability gap as 20-01/20-02's LBL-* IDs. `requirements mark-complete LBL-14` will likely return `not_found`. Add LBL-14 (and all other LBL-* IDs referenced across Phase 20 plans) to REQUIREMENTS.md's traceability table.
- **`pytest` not runnable — sqlalchemy still not installed** — same limitation as 20-01/20-02. `backend/app/domains/labels/service.py` (export_namespace_json/csv additions), `backend/app/domains/labels/router.py` (/export endpoint), and `backend/tests/test_labels_export.py` (7 tests: Tests 1-4 service-level, Tests 5-7 router-level) were verified via `python -m py_compile` (syntax-only). Once `backend/requirements.txt` is installed, run `python -m pytest backend/tests/test_labels_export.py -q` to confirm all 7 tests pass.
- **`backend/app/domains/labels/router.py` shared-file race with concurrent plans 20-03/20-04** — at the time this plan's Task 2 commit (d76293c) was made, `router.py` was untracked in the shared worktree (20-03's base namespace/label CRUD endpoints had not yet been separately committed by 20-03). This plan's commit captured the full router.py content as it existed at that point (20-03's CRUD endpoints + this plan's additive `/export` endpoint) to avoid losing the `/export` endpoint to a subsequent overwrite. 20-04's INVALIDATE_NAMESPACE WS broadcast wiring (verified via test_labels_sdk_router.py's Test 6, which expects `PATCH /labels/keys/{label_id}/value` to call `app.state.ws_manager.broadcast(...)`) was NOT present in router.py at commit time and was not added by this plan (out of scope). If 20-04's WS wiring commit conflicts with or is superseded by d76293c, the phase owner should verify `update_label_value`'s router handler still broadcasts `INVALIDATE_NAMESPACE` after both commits are merged — re-add the wiring if it was dropped.
