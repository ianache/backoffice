---
phase: 21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/g004_seed_login_namespace_labels.py
  - backend/tests/test_labels_sdk_router.py
autonomous: true
requirements: [LOGIN-LBL-01, LOGIN-LBL-08]

must_haves:
  truths:
    - "D-09/D-10/D-11: the eager login namespace contains only authentication-panel copy; BackOffice CC, version, email placeholder, Keycloak proper name, and right-side preview content are not seeded"
    - "D-05/D-06/D-07: login labels are tenant-level values that resolve before authentication without company context"
    - "Both es_PE and en_US bootstrap responses contain the complete login key contract from 21-UI-SPEC.md"
  artifacts:
    - path: "backend/alembic/versions/g004_seed_login_namespace_labels.py"
      provides: "Idempotent eager login namespace and bilingual tenant-level labels"
      contains: "LOGIN_LABELS"
    - path: "backend/tests/test_labels_sdk_router.py"
      provides: "SDK bootstrap regression coverage for login namespace and locale values"
      contains: "test_labels_bootstrap_returns_login_namespace"
  key_links:
    - from: "backend/alembic/versions/g004_seed_login_namespace_labels.py"
      to: "backend/app/domains/sdk/router.py"
      via: "namespace strategy='eager' makes login values part of /labels/bootstrap"
      pattern: "login"
---

<objective>
Create the data contract consumed by the localized login: a dedicated eager `login` namespace with the exact `en_US` and `es_PE` fallback copy approved in `21-UI-SPEC.md`, seeded at tenant level so it resolves before authentication.

Purpose: The Portal cannot hydrate login copy through `LabelClient.initialize()` until the backend bootstrap exposes a complete eager namespace.
Output: Additive seed migration and focused SDK bootstrap tests.
</objective>

<execution_context>
@C:/Users/ianache/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/ianache/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-CONTEXT.md
@.planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-RESEARCH.md
@.planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-UI-SPEC.md
@.planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-VALIDATION.md
@backend/alembic/versions/g002_seed_common_namespace_labels.py
@backend/alembic/versions/g003_repair_labels_tables.py
@backend/app/domains/sdk/router.py
@backend/tests/test_labels_sdk_router.py
</context>

<threat_model>
### T-21-01: Pre-auth seed leaks or scopes sensitive data incorrectly
- **Threat:** Login bootstrap is accessible with the bundled SDK key before authentication; seeded values could accidentally contain credentials, tenant-sensitive content, or company/product overrides.
- **Severity:** HIGH
- **Mitigation:** Seed only the exact public authentication copy listed in `21-UI-SPEC.md`, at tenant level with `company_id=NULL` and `product_id=NULL`; do not seed technical values or secrets.
- **Verification:** Source assertion on the migration label list plus bootstrap tests for tenant-level resolution.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the eager bilingual login namespace seed migration</name>
  <files>backend/alembic/versions/g004_seed_login_namespace_labels.py</files>
  <read_first>
    - backend/alembic/versions/g002_seed_common_namespace_labels.py
    - backend/alembic/versions/g003_repair_labels_tables.py
    - .planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-UI-SPEC.md
    - backend/app/domains/labels/models.py
  </read_first>
  <action>
Create additive revision `g004` with `down_revision = "g003"`; do not edit or rewrite `g001`, `g002`, or `g003`. Follow `g002`'s runtime tenant lookup exactly: prefer tenant id `5`, fall back to the first tenant, and return without error when no tenant exists.

Insert namespace id `login` with strategy `eager` using `INSERT IGNORE`. Define one `LOGIN_LABELS` data structure containing exactly the 16 keys and bilingual values from `21-UI-SPEC.md`: `brand_tagline`, `welcome_title`, `welcome_body`, `sso_action`, `sso_connecting`, `divider_or`, `local_action`, `email_label`, `password_label`, `submit_action`, `submit_loading`, `help_prompt`, `help_action`, `error_invalid_credentials`, `error_authentication_failed`, and `error_generic`.

Insert every value at tenant scope with `company_id=NULL`, `product_id=NULL`, namespace `login`, label type `LABEL`, params `[]`, and version `1`. Use `INSERT IGNORE` for idempotence. Do not include `BackOffice CC`, `admin@backoffice.dev`, `v2.4.12-stable`, right-side preview copy, secrets, or a company/product override. Downgrade deletes only `localized_labels` rows for namespace `login` and then the `login` namespace.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m alembic heads</automated>
  </verify>
  <acceptance_criteria>
    - `backend/alembic/versions/g004_seed_login_namespace_labels.py` contains `revision = "g004"` and `down_revision = "g003"`
    - The migration contains all 16 approved login keys and inserts both `es_PE` and `en_US`
    - Every login insert uses `company_id=NULL` and `product_id=NULL`
    - The migration contains none of `admin@backoffice.dev`, `v2.4.12-stable`, or `dev-sdk-secret`
    - `backend/venv/Scripts/python.exe -m alembic heads` exits 0 and reports `g004`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add SDK bootstrap regression tests for the login namespace</name>
  <files>backend/tests/test_labels_sdk_router.py</files>
  <read_first>
    - backend/tests/test_labels_sdk_router.py
    - backend/app/domains/sdk/router.py
    - backend/app/domains/labels/service.py
    - backend/alembic/versions/g004_seed_login_namespace_labels.py
    - .planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-UI-SPEC.md
  </read_first>
  <action>
Extend the existing SDK router test file with focused tests using its current async SQLite fixtures and helper functions. Prove an eager `login` namespace is returned by `/api/v1/sdk/labels/bootstrap` for `en_US` and `es_PE`, while lazy namespaces remain excluded. Assert representative keys from heading, action, help, and error groups, and assert the fixed/excluded technical and right-preview strings are absent from the returned login namespace.

Add a pre-auth scope case that requests `tenant_id`, `product_id=backoffice`, and no `company_id`, and proves tenant-level login values resolve successfully. Keep SDK auth assertions intact.
  </action>
  <verify>
    <automated>backend/venv/Scripts/python.exe -m pytest backend/tests/test_labels_sdk_router.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_labels_sdk_router.py` contains tests for login bootstrap in both `en_US` and `es_PE`
    - A test requests product `backoffice` without company context and receives tenant-level login labels
    - Tests assert `BackOffice CC`, `admin@backoffice.dev`, and right-side preview copy are absent from the login namespace
    - `backend/venv/Scripts/python.exe -m pytest backend/tests/test_labels_sdk_router.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `backend/venv/Scripts/python.exe -m alembic heads` reports the additive `g004` head
- `backend/venv/Scripts/python.exe -m pytest backend/tests/test_labels_sdk_router.py -q` passes
- Migration inspection confirms only public authentication-panel copy is seeded
</verification>

<success_criteria>
- Dedicated `login` namespace is eager and idempotently seeded for both supported locales
- Login seed resolves at tenant scope without pre-auth company context
- Bootstrap regression tests lock the key contract and scope exclusions
</success_criteria>

<output>
After completion, create `.planning/phases/21-aplicar-el-labeling-en-esta-aplicacion-para-la-pagina-de-ini/21-01-SUMMARY.md`
</output>
